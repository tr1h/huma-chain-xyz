# 📱 Telegram Login Widget Setup Guide

## ✅ Что уже сделано:

1. **Домен привязан к боту:**
   - Выполнена команда `/setdomain` в @BotFather
   - Домен: `solanatamagotchi.com`
   - Бот: `@GotchiGameBot`
   - Результат: ✅ "Success! Domain updated."

---

## 🔧 Как это работает:

### **1. Настройка в BotFather:**
```
/setdomain
→ Ввести домен: solanatamagotchi.com
→ ✅ Success! Domain updated.
```

### **2. Виджет на сайте:**
Виджет автоматически создается на странице `profile.html` с правильными параметрами:

```javascript
<script async src="https://telegram.org/js/telegram-widget.js?22"
    data-telegram-login="GotchiGameBot"
    data-size="large"
    data-onauth="onTelegramAuth(user)"
    data-request-access="write">
</script>
```

### **3. Параметры виджета:**
- **`data-telegram-login`**: `GotchiGameBot` (имя бота без @)
- **`data-size`**: `large` (большая кнопка)
- **`data-onauth`**: `onTelegramAuth(user)` (callback функция)
- **`data-request-access`**: `write` (запрос доступа для отправки сообщений)

---

## 📋 Проверка работы:

### **Шаг 1: Открыть страницу профиля**
```
https://solanatamagotchi.com/profile.html
```

### **Шаг 2: Проверить виджет**
- Должна появиться кнопка "Log in with Telegram"
- Кнопка должна показывать имя бота: `GotchiGameBot`

### **Шаг 3: Авторизация**
1. Нажать на кнопку "Log in with Telegram"
2. Telegram откроет окно авторизации
3. Пользователь подтверждает вход
4. Виджет вызывает `onTelegramAuth(user)` с данными пользователя

### **Шаг 4: Проверка в консоли**
Открыть консоль браузера (F12) и проверить:
```
✅ Telegram Login Widget initialized for: GotchiGameBot
Telegram auth received: {id: ..., username: ..., ...}
✅ Authenticated via Telegram Widget
```

---

## 🔍 Устранение проблем:

### **Проблема 1: Виджет не появляется**
**Решение:**
- Проверить, что домен правильно привязан в BotFather
- Проверить консоль браузера на ошибки
- Убедиться, что скрипт `js/auth.js` загружен

### **Проблема 2: "Domain not linked"**
**Решение:**
- Выполнить `/setdomain` в @BotFather еще раз
- Убедиться, что домен указан без `https://` и `www`
- Правильно: `solanatamagotchi.com`
- Неправильно: `https://solanatamagotchi.com` или `www.solanatamagotchi.com`

### **Проблема 3: Виджет не работает на поддоменах**
**Решение:**
- Telegram Login Widget работает только на точном домене
- Если нужны поддомены, нужно привязать каждый отдельно
- Или использовать основной домен для всех страниц

---

## 📚 Документация Telegram:

Официальная документация: https://core.telegram.org/widgets/login

### **Основные моменты:**
1. **Домен должен быть привязан** через `/setdomain` в @BotFather
2. **Виджет работает только на HTTPS** (кроме localhost)
3. **Callback функция** получает объект с данными пользователя:
   ```javascript
   {
       id: 123456789,
       first_name: "John",
       last_name: "Doe",
       username: "johndoe",
       photo_url: "https://...",
       auth_date: 1234567890,
       hash: "abc123..."
   }
   ```

---

## ✅ Готово!

После привязки домена виджет должен работать автоматически. 

**Проверь:**
1. ✅ Домен привязан в BotFather
2. ✅ Виджет отображается на `profile.html`
3. ✅ Авторизация работает
4. ✅ Данные пользователя сохраняются

**Если всё работает:**
- Пользователи могут авторизоваться через Telegram
- Сессия сохраняется на 7 дней
- Профиль синхронизируется автоматически

