# 🚨 СРОЧНО: Исправление утечек секретов

## ⚠️ Проблема

GitHub Secret Scanning обнаружил утечки:
- **5 Telegram Bot Tokens** в публичном репозитории
- **1 Google API Key** в публичном репозитории

## ✅ Что уже исправлено:

1. ✅ Удалены реальные токены из документации:
   - `bot/SETUP_TOKEN.md` - заменены на `YOUR_TOKEN_FROM_BOTFATHER`
   - `bot/DEPLOY_RENDER.md` - заменены на `YOUR_BOT_TOKEN`
   - `bot/DEPLOY_RAILWAY_WEBHOOK.md` - заменены на `YOUR_BOT_TOKEN`

2. ✅ `.gitignore` уже содержит:
   - `*.log` - все лог файлы
   - `bot/bot_monitoring.log` - конкретный лог файл

## 🔥 ЧТО НУЖНО СДЕЛАТЬ СЕЙЧАС:

### 1. Отозвать скомпрометированные токены

**Для каждого скомпрометированного токена:**

1. Открой @BotFather в Telegram
2. Выбери бота с скомпрометированным токеном
3. Отправь команду: `/revoke`
4. Получи новый токен: `/newtoken`
5. Обнови токен в:
   - Render.com Environment Variables
   - Railway Environment Variables
   - Локальный `.env` файл

**Скомпрометированные токены:**
- `ТОКЕН_УДАЛЁН_20_ДЕК_2025` (старый токен был здесь)
- `ТОКЕН_УДАЛЁН` (частично виден)
- `ТОКЕН_УДАЛЁН` (частично виден)
- `ТОКЕН_УДАЛЁН` (частично виден)

### 2. Отозвать Google API Key (если используется)

1. Открой [Google Cloud Console](https://console.cloud.google.com/)
2. Перейди в **APIs & Services** → **Credentials**
3. Найди ключ `AIzaSyB...`
4. Нажми **Delete** или **Restrict** (ограничить доступ)
5. Создай новый ключ, если нужен

### 3. Удалить лог файлы из Git истории (если они там есть)

```bash
# Проверь, есть ли bot_monitoring.log в Git
git ls-files | grep -i "bot_monitoring.log"

# Если есть, удали из Git (но оставь локально)
git rm --cached bot/bot_monitoring.log
git commit -m "security: Remove bot_monitoring.log from Git"
git push origin main
```

### 4. Проверить другие файлы на утечки

```bash
# Поиск возможных токенов в коде
grep -r "8445221254\|8278463878\|8189046152" . --exclude-dir=.git
```

## 📋 Чеклист безопасности:

- [ ] Отозвать все скомпрометированные Telegram Bot Tokens
- [ ] Создать новые токены через @BotFather
- [ ] Обновить токены в Render.com
- [ ] Обновить токены в Railway (если используется)
- [ ] Обновить локальный `.env` файл
- [ ] Отозвать Google API Key (если используется)
- [ ] Удалить `bot_monitoring.log` из Git истории (если там есть)
- [ ] Проверить, нет ли других секретов в коде

## 🔐 Правила безопасности на будущее:

1. **НИКОГДА не коммить:**
   - Telegram Bot Tokens
   - API Keys
   - Private Keys
   - Passwords
   - Database credentials

2. **ВСЕГДА используй:**
   - Environment Variables (`.env` файлы)
   - `.gitignore` для секретных файлов
   - Placeholder значения в документации (`YOUR_TOKEN_HERE`)

3. **Проверяй перед коммитом:**
   ```bash
   git diff --cached | grep -i "token\|key\|password\|secret"
   ```

## ✅ После исправления:

1. Закрой все Security Alerts в GitHub
2. Проверь, что новые токены работают
3. Убедись, что `.env` файлы в `.gitignore`

---

**ВАЖНО:** Эти токены уже скомпрометированы! Их нужно отозвать **СЕЙЧАС**, иначе кто-то может использовать твоих ботов!

