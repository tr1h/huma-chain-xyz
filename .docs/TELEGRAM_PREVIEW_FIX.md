# 🖼️ TELEGRAM PREVIEW FIX - ИНСТРУКЦИЯ

## ❌ ПРОБЛЕМА: Превью не показывается в Telegram

## ✅ ЧТО СДЕЛАНО:

1. **Обновлены размеры изображения в мета-тегах:**
   - Было: 512x512
   - Стало: 1200x630 (рекомендуемый размер для Telegram)

2. **Добавлен cache-busting:**
   - Используется timestamp для обновления URL изображения
   - Параметр `?v=${timestamp}` добавляется к URL

3. **Улучшена загрузка изображения:**
   - Изображение теперь видно в DOM (opacity: 0.01 вместо 0)
   - Добавлен `crossorigin="anonymous"`
   - Изображение принудительно добавляется в body если его там нет

4. **Обновлены все мета-теги:**
   - `og:image`
   - `og:image:url`
   - `og:image:secure_url`
   - Все обновляются с timestamp для cache-busting

---

## 🔍 ПРОВЕРКА:

### 1. Проверьте доступность изображения:
```bash
curl -I https://solanatamagotchi.com/logo.png
```

Должен вернуть:
- `HTTP/2 200`
- `Content-Type: image/png`
- Размер файла должен быть указан

### 2. Проверьте размер изображения:
Изображение должно быть минимум **200x200** пикселей, лучше **1200x630**.

### 3. Проверьте мета-теги:
Откройте `https://solanatamagotchi.com/s.html?ref=TEST&v=30` и проверьте в DevTools:
- `<meta property="og:image" content="...">` должен содержать правильный URL
- Размеры должны быть `1200x630`

### 4. Проверьте через Telegram Bot API:
```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getUpdates"
```

---

## 🚨 ВАЖНО: TELEGRAM КЕШИРУЕТ ПРЕВЬЮ!

Telegram кеширует превью очень агрессивно. Даже после исправления кода, превью может не обновиться сразу.

### Как обновить кеш Telegram:

1. **Используйте новый URL:**
   - Добавьте уникальный параметр к ссылке
   - Например: `?ref=CODE&v=31` (вместо `v=30`)

2. **Используйте Telegram Bot API для очистки кеша:**
   ```python
   # В боте можно отправить сообщение с новой ссылкой
   # Telegram обновит превью для новой ссылки
   ```

3. **Подождите:**
   - Telegram обновляет кеш периодически
   - Может занять от нескольких минут до нескольких часов

4. **Используйте инструмент для проверки:**
   - https://developers.facebook.com/tools/debug/ (Facebook Debugger)
   - https://cards-dev.twitter.com/validator (Twitter Card Validator)
   - Эти инструменты могут помочь обновить кеш

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ ШАГИ:

### Если превью все еще не работает:

1. **Проверьте что изображение доступно:**
   - Откройте `https://solanatamagotchi.com/logo.png` в браузере
   - Должно открыться изображение

2. **Проверьте размер файла:**
   - Изображение не должно быть слишком большим (>5MB)
   - Рекомендуемый размер: 200KB - 1MB

3. **Проверьте формат:**
   - Должен быть PNG или JPG
   - Не используйте WebP или другие форматы

4. **Проверьте HTTPS:**
   - Изображение должно быть доступно по HTTPS
   - Не должно быть ошибок SSL

5. **Проверьте CORS:**
   - Изображение должно быть доступно для всех
   - Не должно быть ограничений CORS

---

## 📝 ТЕКУЩИЕ НАСТРОЙКИ:

### В `s.html`:
```html
<meta property="og:image" content="https://solanatamagotchi.com/logo.png">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta property="og:image:type" content="image/png">
```

### Изображение в DOM:
```html
<img src="https://solanatamagotchi.com/logo.png" 
     style="position: absolute; width: 1px; height: 1px; opacity: 0.01; ..."
     crossorigin="anonymous">
```

---

## ✅ СЛЕДУЮЩИЕ ШАГИ:

1. Проверьте что `logo.png` существует и доступен
2. Проверьте размер изображения (должен быть минимум 200x200)
3. Попробуйте использовать новый URL с другим параметром `v=`
4. Подождите несколько часов для обновления кеша Telegram
5. Если не помогло, создайте новое изображение специально для превью (1200x630)

---

## 🎯 РЕКОМЕНДАЦИИ:

1. **Создайте специальное изображение для превью:**
   - Размер: 1200x630 пикселей
   - Формат: PNG или JPG
   - Содержание: Логотип + текст "Join Solana Tamagotchi"
   - Сохраните как `preview.png` или `og-image.png`

2. **Используйте это изображение в мета-тегах:**
   ```html
   <meta property="og:image" content="https://solanatamagotchi.com/preview.png">
   ```

3. **Обновите код для использования нового изображения**

