# 🔧 Почему Solscan не показывает изображение NFT, а orb показывает?

## ❌ Проблема:

- **Solscan.io**: Показывает только серый шестиугольник с "NFT" (нет изображения)
- **orb**: Показывает пиксельное изображение Tamagotchi ✅

---

## 🔍 Причины:

### **1. CORS (Cross-Origin Resource Sharing)**

**Проблема:**
- Solscan пытается загрузить изображение из IPFS gateway (`gateway.lighthouse.storage`)
- IPFS gateway может не иметь правильных CORS заголовков для Solscan
- Браузер блокирует загрузку изображения из-за CORS политики

**Решение:**
- Использовать IPFS gateway с правильными CORS заголовками
- Или загрузить изображение на Arweave напрямую (не через IPFS)

---

### **2. Кэширование метаданных**

**Проблема:**
- Solscan кэширует метаданные NFT
- Даже после обновления `json_uri`, Solscan может показывать старые метаданные
- Нужно подождать, пока кэш обновится (5-30 минут)

**Решение:**
- Подождать 5-30 минут после обновления метаданных
- Или использовать другой explorer (orb, Explorer.solana.com)

---

### **3. Разные способы получения изображения**

**orb** может:
- Использовать собственный прокси для IPFS (обход CORS)
- Иметь собственный кэш изображений
- Использовать другой IPFS gateway
- Загружать изображение через свой сервер

**Solscan** может:
- Пытаться загрузить изображение напрямую из IPFS
- Не иметь прокси для обхода CORS
- Требовать определенный формат метаданных

---

### **4. Формат метаданных**

**Проверка:**
```json
{
  "name": "Gotchi Bronze Common #160",
  "symbol": "GOTCHI",
  "description": "...",
  "image": "https://gateway.lighthouse.storage/ipfs/...",  // ✅ Должно быть здесь
  "properties": {
    "files": [
      {
        "uri": "https://gateway.lighthouse.storage/ipfs/...",
        "type": "image/png"
      }
    ]
  }
}
```

---

## ✅ Решения:

### **Вариант 1: Использовать несколько IPFS gateways (быстрое решение)**

Добавить альтернативные IPFS gateways в метаданные:

```javascript
// В api/update-nft-metadata.js
const imageUrl = getNFTImageUrl(tier, rarity);

// Использовать несколько gateways
const ipfsHash = imageUrl.split('/ipfs/')[1]; // Извлечь IPFS hash
const imageUrls = [
    `https://gateway.lighthouse.storage/ipfs/${ipfsHash}`,
    `https://ipfs.io/ipfs/${ipfsHash}`,
    `https://cloudflare-ipfs.com/ipfs/${ipfsHash}`,
    `https://gateway.pinata.cloud/ipfs/${ipfsHash}`
];

// Использовать первый доступный
const metadata = {
    image: imageUrls[0], // Основной URL
    image_alt: imageUrls[1], // Альтернативный
    // ...
};
```

---

### **Вариант 2: Загрузить изображение на Arweave (рекомендуется)**

Вместо IPFS gateway, загрузить изображение на Arweave:

```javascript
// В api/update-nft-metadata.js
// 1. Загрузить изображение на Arweave
const imageResponse = await fetch(imageUrl);
const imageBuffer = await imageResponse.arrayBuffer();
const imageUri = await metaplex.storage().upload(Buffer.from(imageBuffer));

// 2. Использовать Arweave URL в метаданных
const metadata = {
    image: imageUri, // Arweave URL вместо IPFS
    // ...
};
```

**Преимущества:**
- Arweave более надежен для Solscan
- Нет проблем с CORS
- Постоянное хранение
- Solscan лучше работает с Arweave

---

### **Вариант 3: Использовать CDN (лучшее решение)**

Загрузить изображение на собственный CDN:

```javascript
// Загрузить на S3/CDN
const cdnUrl = await uploadToCDN(imageBuffer);
// Использовать cdnUrl в метаданных
```

**Преимущества:**
- Полный контроль над CORS
- Быстрая загрузка
- Надежность
- Работает везде (Solscan, orb, Magic Eden)

---

## 🔍 Проверка:

### **1. Проверить метаданные NFT:**

1. Открыть `json_uri` из Solscan в браузере
2. Проверить поле `"image"` - должен быть валидный URL
3. Открыть URL изображения в браузере - должно загружаться

### **2. Проверить CORS:**

```bash
# В браузере DevTools → Network
# Попробовать загрузить изображение
# Проверить ошибки CORS
```

### **3. Проверить кэш Solscan:**

- Подождать 5-30 минут после обновления
- Очистить кэш браузера
- Попробовать в режиме инкогнито

---

## 📊 Почему orb показывает изображение?

**orb** использует:
1. Собственный прокси для IPFS (обход CORS)
2. Собственный кэш изображений
3. Другой IPFS gateway
4. Загрузку изображения через свой сервер

**Solscan** использует:
1. Прямую загрузку из IPFS
2. Строгие CORS политики
3. Кэширование метаданных

---

## 🎯 Рекомендация:

**Использовать Arweave для изображений** вместо IPFS gateway:

1. Загрузить изображение на Arweave через Metaplex
2. Использовать Arweave URL в метаданных
3. Solscan сможет загрузить изображение без проблем с CORS

**Или:**
- Использовать несколько IPFS gateways
- Загрузить изображение на собственный CDN

---

## 🔗 Полезные ссылки:

- [Solscan NFT Metadata Format](https://docs.solscan.io/)
- [Metaplex Metadata Standard](https://docs.metaplex.com/programs/token-metadata/token-standard)
- [IPFS Gateway List](https://ipfs.github.io/public-gateway-checker/)
