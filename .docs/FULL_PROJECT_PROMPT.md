# 🎮 SOLANA TAMAGOTCHI - ПОЛНЫЙ ПРОМПТ ПРОЕКТА

> **Для AI-ассистентов и новых разработчиков:** Полное описание проекта, архитектуры, текущего состояния и задач

**Дата обновления:** 14 ноября 2025  
**Версия:** 2.0  
**Статус:** Production (Devnet)

---

## 📋 ЧТО ЭТО ЗА ПРОЕКТ?

**Solana Tamagotchi** — это Play-to-Earn игра в Telegram с интеграцией блокчейна Solana. Игроки ухаживают за виртуальными питомцами (Gotchi), зарабатывают токены TAMA и могут выводить их в реальные кошельки Solana.

### Ключевые особенности:
- 🎮 **Игра в Telegram** — через бота `@GotchiGameBot` или веб-интерфейс
- 💰 **TAMA Token** — SPL токен на Solana Devnet (1 миллиард токенов)
- 🖼️ **NFT система** — 3-тирная система (Bronze/Silver/Gold) с bonding curve
- 🎯 **Play-to-Earn** — клики, квесты, рефералы, достижения
- 🔥 **On-Chain** — реальные транзакции Solana для вывода и NFT минта
- ⚙️ **Admin Panels** — централизованное управление экономикой игры
- 🛡️ **Security** — серверная валидация, RLS, защита от читерства

---

## 🏗️ АРХИТЕКТУРА ПРОЕКТА

```
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND (GitHub Pages)                   │
│  https://solanatamagotchi.com/                                │
│                                                              │
│  • tamagotchi-game.html    - Основная игра                  │
│  • mint.html                - Минт NFT (3 тира)             │
│  • admin-table.html         - Админ панель пользователей    │
│  • super-admin.html         - Супер админ панель             │
│  • transactions-admin.html  - Админ панель транзакций        │
│  • economy-admin.html       - Панель управления экономикой   │
│  • wallet-admin.html        - Управление кошельками         │
└──────────────┬──────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│              BACKEND API (Render.com - PHP)                  │
│  https://api.solanatamagotchi.com/api/tama/                  │
│                                                              │
│  • tama_supabase.php        - Главный API (REST)             │
│  • router.php                - Роутер для маршрутизации       │
│  • mint-nft-*.php            - NFT минт эндпоинты            │
│  • analyze-user.php          - Анализ пользователей          │
└──────────────┬──────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│              TELEGRAM BOT (Render.com - Python)             │
│  https://huma-chain-xyz-bot.onrender.com/                     │
│                                                              │
│  • bot.py                    - Telegram бот (@GotchiGameBot) │
│  • gamification.py           - Геймификация                  │
│  • nft_system.py             - NFT система                   │
└──────────────┬──────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│              DATABASE (Supabase PostgreSQL)                  │
│  https://zfrazyupameidxpjihrh.supabase.co/                    │
│                                                              │
│  • leaderboard               - Пользователи и балансы        │
│  • transactions              - История транзакций             │
│  • user_nfts                 - NFT пользователей             │
│  • referrals                 - Реферальная система           │
│  • nft_bonding_state         - Состояние NFT минта           │
└──────────────┬──────────────────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────────────────┐
│              BLOCKCHAIN (Solana Devnet)                      │
│                                                              │
│  • TAMA Token (SPL)          - Fuqw8Zg17XhHGXfghLYD1fqjxJa1PnmG2MmoqG5pcmLY │
│  • NFT Collection            - Metaplex                      │
│  • On-Chain Transfers        - SPL token transfers           │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
huma-chain-xyz/
│
├── 🌐 FRONTEND (GitHub Pages - все в корне!)
│   ├── tamagotchi-game.html      # ⭐ Основная игра (403KB)
│   ├── mint.html                 # NFT минт страница
│   ├── admin-table.html          # Админ панель пользователей
│   ├── super-admin.html          # Супер админ панель
│   ├── transactions-admin.html   # Админ панель транзакций
│   ├── economy-admin.html        # Панель управления экономикой
│   ├── wallet-admin.html         # Управление кошельками
│   ├── check-user.html           # Проверка пользователей
│   ├── referral.html             # Реферальная система
│   ├── index.html                # Главная страница
│   └── [10+ других HTML страниц]
│
├── 🛠️ BACKEND API
│   ├── api/
│   │   ├── tama_supabase.php     # ⭐ Главный API (REST)
│   │   ├── router.php            # Роутер для маршрутизации
│   │   ├── load_keypairs.php     # Загрузка keypairs из env
│   │   ├── mint-nft-bronze.php   # Минт Bronze NFT за TAMA
│   │   ├── mint-nft-sol.php      # Минт NFT за SOL
│   │   ├── mint-nft-onchain.js   # Node.js on-chain минт
│   │   ├── analyze-user.php      # Анализ пользователей
│   │   ├── check-duplicate-transactions.php
│   │   └── [другие эндпоинты]
│   │
│   ├── bot/
│   │   ├── bot.py                # Telegram бот
│   │   ├── gamification.py       # Геймификация
│   │   ├── nft_system.py         # NFT система
│   │   └── requirements.txt      # Python зависимости
│   │
│   └── sql/
│       ├── fix-leaderboard-rls-security.sql  # RLS защита
│       ├── create_nft_5tier_system.sql
│       └── [другие SQL скрипты]
│
├── 🔧 CONFIG
│   ├── package.json              # Node.js зависимости
│   ├── package-lock.json
│   ├── render.yaml               # Render.com конфигурация
│   ├── Dockerfile                # Docker конфигурация
│   └── .htaccess                 # Apache конфигурация
│
├── 🔐 SECURITY
│   ├── admin-password.js         # Локальный пароль админа (gitignored)
│   ├── js/admin-auth.js          # Система аутентификации админов
│   └── wallet-admin-password.js # Пароль для wallet-admin
│
└── 📚 DOCUMENTATION (.docs/)
    ├── README.md
    ├── FULL_PROJECT_PROMPT.md    # Этот файл
    └── [20+ других документов]
```

---

## 🔌 API ENDPOINTS

### **Base URL:** `https://api.solanatamagotchi.com/api/tama`

### **Основные эндпоинты:**

#### **1. Баланс и пользователи:**
- `GET /balance?telegram_id=XXX` - Получить баланс пользователя
- `POST /leaderboard/upsert` - Обновить/создать пользователя
  - **Важно:** Имеет серверную валидацию:
    - Максимальное увеличение за запрос: 10,000 TAMA
    - Дневной лимит заработка: 10,000 TAMA/день
    - Защита от отрицательных балансов
    - Логирование подозрительных действий
- `GET /leaderboard` - Получить таблицу лидеров
- `GET /leaderboard/list` - Список пользователей

#### **2. Транзакции:**
- `GET /transactions/list?telegram_id=XXX` - История транзакций
- `POST /add` - Добавить TAMA (устаревший, используйте `/leaderboard/upsert`)
- `POST /spend` - Потратить TAMA (устаревший, используйте `/leaderboard/upsert`)

#### **3. NFT:**
- `POST /mint-nft` - Минт NFT (устаревший)
- `POST /api/mint-nft-bronze` - Минт Bronze NFT за TAMA (5000 TAMA)
- `POST /api/mint-nft-sol` - Минт NFT за SOL
- `POST /api/mint-nft-onchain` - On-chain минт через Node.js

#### **4. Вывод средств:**
- `POST /withdrawal/request` - Запрос на вывод TAMA
- `GET /withdrawal/history?telegram_id=XXX` - История выводов

#### **5. Админ функции:**
- `POST /send` - Отправить TAMA (админ)
- `POST /send-sol` - Отправить SOL (админ)
- `POST /mint` - Минт токенов (админ)
- `GET /holders` - Список держателей токенов
- `GET /pools` - Статистика пулов

#### **6. Экономика:**
- `POST /economy/apply` - Применить изменения экономики
- `GET /economy/active` - Получить активную экономику

#### **7. Утилиты:**
- `GET /test` - Health check
- `GET /env-check` - Проверка переменных окружения
- `GET /stats` - Статистика

#### **8. Анализ:**
- `GET /api/analyze-user?username=XXX` - Анализ пользователя
- `GET /api/check-duplicate-transactions` - Проверка дубликатов

---

## 🗄️ БАЗА ДАННЫХ (SUPABASE)

### **Таблица `leaderboard`:**
```sql
- id (bigint, PK)
- telegram_id (text, UNIQUE)
- telegram_username (text)
- wallet_address (text)
- tama (numeric)              -- Баланс TAMA
- level (int)
- xp (int)
- pet_name (text)
- pet_type (text)
- pet_data (jsonb)
- created_at (timestamp)
- updated_at (timestamp)
```

**RLS (Row Level Security):**
- ✅ `anon` может **читать** (SELECT)
- ❌ `anon` **НЕ может** обновлять (UPDATE/PATCH)
- ✅ Только `service_role` (API) может обновлять баланс
- ✅ `authenticated` может создавать (INSERT) новые записи

**SQL скрипт:** `sql/fix-leaderboard-rls-security.sql`

### **Таблица `transactions`:**
```sql
- id (bigint, PK)
- user_id (text)              -- telegram_id пользователя
- username (text)
- type (text)                 -- earn_click, spend_feed, nft_mint_tama, etc.
- amount (numeric)            -- Положительное или отрицательное
- balance_before (numeric)     -- Баланс до транзакции
- balance_after (numeric)     -- Баланс после транзакции (КРИТИЧНО!)
- metadata (jsonb)            -- Дополнительные данные
- created_at (timestamp)
```

**Типы транзакций:**
- `earn_click` - Заработок за клик
- `spend_feed` - Потрачено на кормление
- `spend_play` - Потрачено на игру
- `spend_heal` - Потрачено на лечение
- `nft_mint_tama` - Минт NFT за TAMA
- `nft_mint_sol` - Минт NFT за SOL
- `nft_mint_onchain` - On-chain минт NFT
- `withdrawal` - Вывод средств
- `referral_bonus` - Реферальный бонус
- `quest_complete` - Завершение квеста
- `achievement_bonus` - Бонус за достижение
- `daily_reward` - Ежедневная награда

### **Таблица `user_nfts`:**
```sql
- id (bigint, PK)
- telegram_id (text)
- wallet_address (text)
- mint_address (text)         -- On-chain адрес NFT
- tier_id (int)               -- 1=Bronze, 2=Silver, 3=Gold
- tier_name (text)
- rarity_id (int)             -- 1=Common, 2=Uncommon, 3=Rare, 4=Epic, 5=Legendary
- design_id (int)
- boost_multiplier (numeric)  -- Множитель заработка
- price_paid_tama (numeric)
- price_paid_sol (numeric)
- payment_type (text)         -- TAMA или SOL
- is_active (boolean)
- created_at (timestamp)
```

### **Таблица `referrals`:**
```sql
- id (bigint, PK)
- referrer_telegram_id (text)
- referred_telegram_id (text)
- status (text)                -- active, pending
- reward_paid (boolean)
- created_at (timestamp)
```

### **Таблица `nft_bonding_state`:**
```sql
- id (bigint, PK)
- tier_name (text)            -- Bronze, Silver, Gold
- payment_type (text)         -- TAMA, SOL
- minted_count (int)
- max_supply (int)
- current_price (numeric)
- created_at (timestamp)
- updated_at (timestamp)
```

---

## 🛡️ БЕЗОПАСНОСТЬ

### **1. Серверная валидация (API):**
**Файл:** `api/tama_supabase.php` → `handleLeaderboardUpsert()`

**Защита:**
- ✅ Максимальное увеличение за запрос: **10,000 TAMA**
- ✅ Дневной лимит заработка: **10,000 TAMA/день**
- ✅ Защита от отрицательных балансов
- ✅ Логирование подозрительных действий (>1,000 TAMA)
- ✅ Админские исправления (`skip_transaction_log: true`) обходят лимиты, но логируются

**Код:**
```php
$MAX_INCREASE_PER_REQUEST = 10000;
$DAILY_LIMIT = 10000; // Проверяется через transactions table

// Проверка дневного лимита
$todayEarned = queryTransactions($user_id, 'earn_click', $today);
if ($todayEarned + $tamaDiff > $DAILY_LIMIT && !$skip_transaction_log) {
    // Ограничить до лимита
}
```

### **2. Row Level Security (RLS):**
**Файл:** `sql/fix-leaderboard-rls-security.sql`

**Защита:**
- ✅ `anon` может только **читать** данные
- ❌ `anon` **НЕ может** обновлять баланс напрямую
- ✅ Только `service_role` (API) может обновлять баланс
- ✅ Предотвращает читерство через прямой доступ к Supabase

### **3. Аутентификация админов:**
**Файлы:**
- `js/admin-auth.js` - Централизованная система аутентификации
- `admin-password.js` - Локальный пароль (gitignored)

**Защита:**
- ✅ Пароли хешируются (SHA-256)
- ✅ Сессионное управление (sessionStorage)
- ✅ Защита от брутфорса (3 попытки, блокировка на 5 минут)
- ✅ Логирование попыток входа
- ✅ В продакшене используются хеши паролей в meta tags

**Защищенные страницы:**
- `super-admin.html`
- `admin-table.html`
- `transactions-admin.html`
- `economy-admin.html`
- `blog-admin.html`
- `admin-tokenomics.html`
- `admin-nft-tiers.html`
- `admin-dashboard.html`
- `wallet-admin.html`

### **4. CORS:**
**Файл:** `api/tama_supabase.php`

**Разрешенные домены:**
- `https://solanatamagotchi.com`
- `https://www.solanatamagotchi.com`
- `https://tr1h.github.io`
- `http://localhost` (для разработки)

---

## 🚀 ДЕПЛОЙ

### **Frontend (GitHub Pages):**
- **URL:** https://solanatamagotchi.com/
- **Деплой:** Автоматический при push в `main` branch
- **Настройка:** GitHub Pages → Source: `main` branch

### **Backend API (Render.com):**
- **URL:** https://api.solanatamagotchi.com/
- **Тип:** Web Service (PHP + Apache)
- **Конфигурация:** `render.yaml`
- **Environment Variables:**
  ```env
  SUPABASE_URL=https://zfrazyupameidxpjihrh.supabase.co
  SUPABASE_KEY=your_service_key
  TAMA_MINT_ADDRESS=Fuqw8Zg17XhHGXfghLYD1fqjxJa1PnmG2MmoqG5pcmLY
  SOLANA_RPC_URL=https://api.devnet.solana.com
  SOLANA_PAYER_KEYPAIR={"keypair":"json"}
  SOLANA_P2E_POOL_KEYPAIR={"keypair":"json"}
  ```

### **Telegram Bot (Render.com):**
- **URL:** https://huma-chain-xyz-bot.onrender.com/
- **Тип:** Web Service (Python + Flask)
- **Режим:** Webhook (не polling!)
- **Keep-Alive:** Пинг каждые 5 минут
- **Environment Variables:**
  ```env
  TELEGRAM_BOT_TOKEN=your_bot_token
  BOT_USERNAME=GotchiGameBot
  GAME_URL=https://solanatamagotchi.com/tamagotchi-game.html
  TAMA_API_BASE=https://api.solanatamagotchi.com/api/tama
  SUPABASE_URL=your_url
  SUPABASE_KEY=your_anon_key
  RENDER=true
  ```

### **Node.js On-Chain Service (Render.com):**
- **URL:** https://solanatamagotchi-onchain.onrender.com/
- **Тип:** Web Service (Node.js)
- **Назначение:** On-chain минт NFT через Metaplex SDK
- **Файл:** `api/mint-nft-onchain.js`

---

## 💰 ТОКЕНОМИКА

### **TAMA Token:**
- **Mint Address:** `Fuqw8Zg17XhHGXfghLYD1fqjxJa1PnmG2MmoqG5pcmLY`
- **Network:** Solana Devnet
- **Total Supply:** 1,000,000,000 TAMA
- **Decimals:** 9

### **Распределение:**
- **P2E Pool:** 400M (40%) - Награды игрокам
- **Team:** 200M (20%) - Команда (vesting 4 года)
- **Marketing:** 150M (15%) - Маркетинг
- **Liquidity:** 100M (10%) - Ликвидность DEX
- **Community:** 100M (10%) - Сообщество
- **Reserve:** 50M (5%) - Резерв

### **NFT Система:**
- **Bronze:** 2,500 TAMA или 0.05 SOL → 2-3x boost
- **Silver:** 0.1 SOL → 2.5-3.5x boost
- **Gold:** 0.2 SOL → 3-4x boost

**Распределение доходов от NFT:**
- 40% → Burn (уничтожение)
- 30% → Treasury (казначейство)
- 30% → P2E Pool (возврат игрокам)

---

## 🔧 ТЕХНОЛОГИЧЕСКИЙ СТЕК

### **Frontend:**
- HTML5, CSS3, JavaScript (Vanilla)
- Solana Web3.js
- Phantom Wallet Integration
- Tailwind CSS (CDN)

### **Backend API:**
- PHP 8.2
- Apache
- Supabase REST API
- Solana CLI (`spl-token`)

### **Telegram Bot:**
- Python 3.10+
- python-telegram-bot
- Flask (для webhook)
- Supabase Python Client

### **On-Chain Service:**
- Node.js 18+
- Metaplex SDK (`@metaplex-foundation/js`)
- Solana Web3.js
- Bundlr Storage (Arweave)

### **Database:**
- Supabase (PostgreSQL)
- Row Level Security (RLS)
- REST API (не прямые SQL подключения)

### **Blockchain:**
- Solana Devnet
- SPL Tokens
- Metaplex NFT Standard

---

## 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### **1. Баланс сбрасывается до 0:**
**Проблема:** При расчете баланса из транзакций используется `balance_after` из последней транзакции, но если он равен 0, баланс сбрасывается.

**Решение:** В `admin-table.html` используется двухэтапный расчет:
1. Сначала берется `balance_after` из последней транзакции
2. Если его нет или он 0, рассчитывается с нуля суммированием всех транзакций

**Файл:** `admin-table.html` (строки 510-557)

### **2. Читерство через прямой доступ к Supabase:**
**Проблема:** Пользователи могли обновлять баланс напрямую через `SUPABASE_ANON_KEY`.

**Решение:**
1. ✅ RLS политики запрещают `anon` обновлять баланс
2. ✅ Все обновления идут через API с валидацией
3. ✅ `tamagotchi-game.html` и `admin-table.html` используют API вместо прямого доступа

**Файлы:**
- `sql/fix-leaderboard-rls-security.sql`
- `tamagotchi-game.html` (использует `/api/tama/leaderboard/upsert`)
- `admin-table.html` (использует `/api/tama/leaderboard/upsert`)

### **3. Транзакции `nft_mint_tama` показывают `balance_after = 0`:**
**Проблема:** В транзакциях типа `nft_mint_tama` поле `balance_after` записывается как 0.

**Решение:** Нужно проверить `api/mint-nft-bronze.php` и убедиться, что `balance_after` правильно вычисляется и записывается.

**Файл:** `api/mint-nft-bronze.php` (строки 183-199)

---

## 📝 КЛЮЧЕВЫЕ ФАЙЛЫ И ИХ НАЗНАЧЕНИЕ

### **Frontend:**
- `tamagotchi-game.html` - Основная игра, логика заработка TAMA
- `mint.html` - Страница минта NFT
- `admin-table.html` - Админ панель для управления пользователями
- `check-user.html` - Анализ пользователей на подозрительную активность

### **Backend:**
- `api/tama_supabase.php` - Главный API, все эндпоинты, серверная валидация
- `api/router.php` - Роутер для маршрутизации запросов
- `api/analyze-user.php` - Анализ пользователей для выявления читерства
- `bot/bot.py` - Telegram бот, обработка команд

### **Security:**
- `js/admin-auth.js` - Система аутентификации админов
- `sql/fix-leaderboard-rls-security.sql` - RLS политики для защиты базы

---

## 🎯 ТЕКУЩЕЕ СОСТОЯНИЕ

### **✅ Реализовано:**
- ✅ Полная игра с заработком TAMA
- ✅ NFT система (3 тира)
- ✅ Реферальная система
- ✅ Вывод средств на Solana
- ✅ Админ панели
- ✅ Серверная валидация балансов
- ✅ RLS защита базы данных
- ✅ Аутентификация админов
- ✅ Анализ подозрительной активности
- ✅ On-chain минт NFT

### **🚧 В разработке:**
- 🔄 Исправление записи `balance_after` в транзакциях `nft_mint_tama`
- 🔄 Улучшение анализа пользователей

### **📋 TODO:**
- [ ] Проверить и исправить запись `balance_after` в `api/mint-nft-bronze.php`
- [ ] Добавить автоматическое исправление балансов на основе транзакций
- [ ] Улучшить UI для админ панелей

---

## 🔑 ВАЖНЫЕ КОНСТАНТЫ И АДРЕСА

### **TAMA Token:**
- **Mint:** `Fuqw8Zg17XhHGXfghLYD1fqjxJa1PnmG2MmoqG5pcmLY`
- **Network:** Devnet
- **Decimals:** 9

### **URLs:**
- **Frontend:** https://solanatamagotchi.com/
- **API:** https://api.solanatamagotchi.com/api/tama
- **Bot:** https://huma-chain-xyz-bot.onrender.com/
- **On-Chain:** https://solanatamagotchi-onchain.onrender.com/

### **Supabase:**
- **URL:** https://zfrazyupameidxpjihrh.supabase.co
- **Project ID:** `zfrazyupameidxpjihrh`

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

- **README.md** - Основная документация проекта
- **.docs/SYSTEM_ARCHITECTURE.md** - Детальная архитектура
- **.docs/HOW_IT_WORKS.md** - Как работает система
- **.docs/API_AND_DISPLAY_FIXES.md** - Исправления API
- **.docs/ADDRESSES_AND_ALLOCATIONS.md** - Адреса и распределение токенов

---

## 🆘 КАК НАЧАТЬ РАБОТУ С ПРОЕКТОМ

### **Для новых разработчиков:**
1. Прочитай этот файл полностью
2. Изучи структуру проекта
3. Настрой локальное окружение
4. Прочитай документацию в `.docs/`
5. Изучи код основных файлов:
   - `api/tama_supabase.php` - API логика
   - `tamagotchi-game.html` - Игровая логика
   - `bot/bot.py` - Telegram бот

### **Для AI-ассистентов:**
1. Используй этот промпт как контекст
2. Всегда проверяй текущее состояние через `codebase_search`
3. При изменении балансов используй API (`/api/tama/leaderboard/upsert`)
4. При работе с безопасностью проверяй RLS политики
5. При анализе пользователей используй `api/analyze-user.php`

---

**Версия:** 2.0  
**Последнее обновление:** 14 ноября 2025  
**Автор:** Solana Tamagotchi Team
