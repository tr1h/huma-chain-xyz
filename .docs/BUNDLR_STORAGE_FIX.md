# 🔧 Исправление Bundlr Storage (MockStorage проблема)

## ❌ Проблема:

В логах Render.com видно:
```
✅ Metaplex initialized with Mock storage
✅ New metadata uploaded: https://mockstorage.example.com/...
```

Это означает, что **Bundlr не инициализируется**, и используется **mockStorage** вместо реального Arweave.

## 🔍 Причины:

1. **`bundlrStorage` не экспортируется** из `@metaplex-foundation/js` v0.20.1
2. **Bundlr инициализация падает** с ошибкой, но код использует fallback на mockStorage
3. **Недостаточно SOL** на payer keypair для Arweave fees

## ✅ Решение:

### **1. Проверить экспорт bundlrStorage:**

В `api/update-nft-metadata.js` добавлена диагностика:
```javascript
console.log('   bundlrStorage type:', typeof bundlrStorage);
console.log('   bundlrStorage value:', bundlrStorage ? 'exists' : 'undefined');
```

### **2. Обновить @metaplex-foundation/js:**

Возможно, нужно обновить пакет:
```bash
npm install @metaplex-foundation/js@latest
```

Или использовать альтернативный storage provider.

### **3. Проверить баланс payer:**

Payer keypair должен иметь минимум **0.01 SOL** для Arweave fees:
```
💰 Payer balance: 4.181557499 SOL ✅ (достаточно)
```

### **4. Проверить Bundlr devnet:**

Bundlr devnet должен быть доступен:
- URL: `https://devnet.bundlr.network`
- Проверить статус: https://devnet.bundlr.network/info

---

## 🚨 Временное решение:

Если Bundlr не работает, можно использовать **альтернативные storage providers**:

1. **IPFS через NFT.Storage:**
   - Уже установлен: `nft.storage`
   - Можно использовать для хранения метаданных

2. **Arweave напрямую:**
   - Использовать `@irys/sdk` для прямого доступа к Arweave
   - Уже есть в `package.json` overrides

3. **Проверить версию Metaplex:**
   - Возможно, в v0.20.1 `bundlrStorage` работает по-другому
   - Нужно проверить документацию

---

## 📋 Что проверить:

1. ✅ Логи Render.com показывают тип `bundlrStorage`
2. ✅ Баланс payer достаточен (4.18 SOL ✅)
3. ✅ Bundlr devnet доступен
4. ✅ `@metaplex-foundation/js` версия поддерживает Bundlr

---

## 🔄 Следующие шаги:

1. **Проверить логи** после деплоя исправленного кода
2. **Увидеть диагностику** типа `bundlrStorage`
3. **Если все еще mockStorage** → обновить пакет или использовать альтернативу

