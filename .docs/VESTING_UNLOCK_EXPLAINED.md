# 🔓 КАК РАБОТАЕТ РАЗБЛОКИРОВКА В VESTING STREAM

## ❓ ВОПРОС: "После разблокировки куда то токены падают? Или просто появляется возможность для вывода?"

**ОТВЕТ:** Токены **НЕ падают автоматически**! Они остаются в **escrow account** (заблокированном счете) и становятся **доступными для вывода** по мере разблокировки.

---

## 🎯 КАК ЭТО РАБОТАЕТ

### **1. Создание Stream (сейчас):**

```
Team Wallet (sender)
├─ Отправляет: 200M TAMA
└─ В: Escrow Account (Streamflow contract)
   └─ Статус: 🔒 ЗАБЛОКИРОВАНО
```

**Что происходит:**
- ✅ Токены **физически переходят** в escrow account
- ✅ Team wallet **теряет доступ** к токенам
- ✅ Токены **заблокированы** в контракте Streamflow
- ✅ **Нельзя** отправить даже с keypair

---

### **2. Период Cliff (0-6 месяцев):**

```
Escrow Account
├─ Баланс: 200M TAMA
├─ Разблокировано: 0 TAMA ❌
├─ Доступно для вывода: 0 TAMA ❌
└─ Статус: 🔒 ПОЛНАЯ БЛОКИРОВКА
```

**Что происходит:**
- ✅ Токены **все еще в escrow**
- ✅ **Ничего** не разблокировано
- ✅ **Нельзя** вывести
- ✅ Team wallet **не может** получить токены

---

### **3. После Cliff (6+ месяцев):**

```
Escrow Account
├─ Баланс: 200M TAMA (все еще в escrow)
├─ Разблокировано: ~4.76M TAMA ✅ (постепенно)
├─ Доступно для вывода: ~4.76M TAMA ✅
└─ Статус: 🔓 ЧАСТИЧНАЯ РАЗБЛОКИРОВКА
```

**Что происходит:**
- ✅ Токены **все еще в escrow**
- ✅ Но теперь **часть разблокирована**
- ✅ Team wallet **может вывести** разблокированную часть
- ✅ **Не разблокированные** токены остаются в escrow

---

### **4. Процесс Вывода:**

```
Team Wallet вызывает withdraw():
├─ Streamflow проверяет: сколько разблокировано?
├─ Если есть разблокированные токены:
│  ├─ Streamflow переводит их из escrow
│  └─ В: Team Wallet token account ✅
└─ Если нет разблокированных:
   └─ Ошибка: "Nothing to withdraw"
```

**Важно:**
- ✅ Вывод **НЕ автоматический** - нужно вызывать `withdraw()`
- ✅ Можно выводить **в любое время** (сколько разблокировано)
- ✅ Можно выводить **частично** или **полностью** (разблокированную часть)
- ✅ **Не разблокированные** токены остаются в escrow

---

## 📊 ПРИМЕР РАСПИСАНИЯ

### **Месяц 0-6 (Cliff):**

```
Escrow: 200M TAMA
├─ Разблокировано: 0 TAMA ❌
├─ Доступно: 0 TAMA ❌
└─ Team Wallet: 0 TAMA (не может вывести)
```

### **Месяц 7 (после Cliff):**

```
Escrow: 200M TAMA
├─ Разблокировано: ~4.76M TAMA ✅
├─ Доступно: ~4.76M TAMA ✅
└─ Team Wallet: 0 TAMA (но МОЖЕТ вывести 4.76M)
```

**После вывода:**

```
Escrow: ~195.24M TAMA (осталось)
├─ Разблокировано: ~4.76M TAMA ✅ (уже выведено)
├─ Доступно: 0 TAMA (выведено)
└─ Team Wallet: 4.76M TAMA ✅ (получено)
```

### **Месяц 12 (через год):**

```
Escrow: ~180M TAMA (осталось)
├─ Разблокировано: ~23.81M TAMA ✅ (накоплено)
├─ Доступно: ~23.81M TAMA ✅
└─ Team Wallet: 4.76M TAMA (если выводил в месяц 7)
```

**После вывода всех накопленных:**

```
Escrow: ~180M TAMA (осталось)
├─ Разблокировано: ~23.81M TAMA ✅ (выведено)
├─ Доступно: 0 TAMA
└─ Team Wallet: 28.57M TAMA ✅ (4.76M + 23.81M)
```

---

## 🔄 ДВА СЦЕНАРИЯ

### **Сценарий 1: Автоматический вывод (если настроено)**

```
Streamflow автоматически выводит разблокированные токены:
├─ Каждый период (например, каждый день)
├─ Автоматически переводит в Team Wallet
└─ НО: нужно настроить automaticWithdrawal: true
```

**В нашем случае:**
- ❌ `automaticWithdrawal: false`
- ✅ **Нужно выводить вручную**

---

### **Сценарий 2: Ручной вывод (наш случай)**

```
Team Wallet вызывает withdraw():
├─ Проверяет сколько разблокировано
├─ Переводит разблокированную часть
└─ Остальное остается в escrow
```

**Как вывести:**

```javascript
// Через SDK
await streamflowClient.withdraw(streamId, solanaWithdrawParams);

// Или через веб-интерфейс Streamflow
// https://streamflow.finance → My Streams → Withdraw
```

---

## 💡 ИТОГО

### **Ответ на вопрос:**

```
❌ Токены НЕ падают автоматически
✅ Они остаются в escrow account
✅ Появляется возможность вывода
✅ Нужно вызывать withdraw() вручную
✅ Можно выводить частично или полностью
✅ Не разблокированные остаются в escrow
```

### **Где находятся токены:**

```
До разблокировки:
├─ Escrow Account: 200M TAMA 🔒
└─ Team Wallet: 0 TAMA

После разблокировки (но до вывода):
├─ Escrow Account: 200M TAMA (часть разблокирована) 🔓
└─ Team Wallet: 0 TAMA (но может вывести разблокированную часть)

После вывода:
├─ Escrow Account: ~195.24M TAMA (осталось) 🔒
└─ Team Wallet: 4.76M TAMA ✅ (выведено)
```

---

## 📋 ПРАКТИЧЕСКИЙ ПРИМЕР

### **Шаг 1: Создание Stream (сейчас)**

```bash
# Токены переходят в escrow
Team Wallet: 200M TAMA → Escrow: 200M TAMA
```

### **Шаг 2: Через 6 месяцев (Cliff прошел)**

```bash
# Часть разблокирована, но еще в escrow
Escrow: 200M TAMA (4.76M разблокировано, доступно для вывода)
Team Wallet: 0 TAMA (но может вывести 4.76M)
```

### **Шаг 3: Вывод разблокированной части**

```bash
# Team Wallet вызывает withdraw()
Escrow: 195.24M TAMA (осталось)
Team Wallet: 4.76M TAMA ✅ (получено)
```

### **Шаг 4: Продолжение разблокировки**

```bash
# Каждую секунду разблокируется еще немного
# Team Wallet может выводить накопленное в любое время
```

---

**Готово! Теперь понятно как работает разблокировка!** 🔓

