# 🔄 Работа со второго компьютера

## 📥 Первая настройка (клонирование)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/tr1h/huma-chain-xyz.git

# 2. Перейти в папку проекта
cd huma-chain-xyz

# 3. Проверить текущую ветку
git branch
```

## 🔄 Ежедневная синхронизация

### Получить последние изменения с GitHub:
```bash
git pull origin main
```

### Отправить свои изменения на GitHub:
```bash
# 1. Посмотреть, что изменилось
git status

# 2. Добавить все изменения
git add .

# 3. Закоммитить с описанием
git commit -m "Описание ваших изменений"

# 4. Отправить на GitHub
git push origin main
```

## 📋 Полезные команды

### Проверить статус:
```bash
git status
```

### Посмотреть изменения:
```bash
git diff
```

### Посмотреть историю коммитов:
```bash
git log --oneline
```

### Отменить локальные изменения (если что-то сломалось):
```bash
git restore <имя_файла>
```

### Полностью перезагрузить с GitHub (если локальные изменения не нужны):
```bash
git fetch origin
git reset --hard origin/main
```

## ⚠️ Важно!

1. **Всегда делайте `git pull` перед началом работы** - чтобы получить последние изменения
2. **Не коммитьте чувствительные файлы:**
   - `ADMIN_PASSWORDS.txt`
   - `admin-password.js`
   - `wallet-admin-password.js`
   - `wallet-admin.html`
   - `*.keypair.json`
   - `.env` файлы

3. **Перед push проверяйте:**
   ```bash
   git status
   ```
   Убедитесь, что нет чувствительных файлов в staging!

## 🔐 Настройка Git (если еще не настроено)

```bash
# Установить имя
git config --global user.name "Ваше Имя"

# Установить email
git config --global user.email "ваш@email.com"

# Проверить настройки
git config --list
```

## 🚀 Быстрый старт

```bash
# Каждый раз перед работой:
git pull origin main

# После внесения изменений:
git add .
git commit -m "Описание изменений"
git push origin main
```

## 📚 Дополнительно

- **GitHub репозиторий:** https://github.com/tr1h/huma-chain-xyz
- **Основная ветка:** `main`
- **Автодеплой:** GitHub Actions автоматически деплоит на GitHub Pages при push в `main`







