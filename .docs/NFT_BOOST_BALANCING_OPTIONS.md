# ⚖️ ВАРИАНТЫ БАЛАНСИРОВКИ NFT БУСТОВ

## 📊 ТЕКУЩАЯ СИТУАЦИЯ

**Из логов:**
- `BASE_CLICK_REWARD: 20` - базовый клик дает 20 TAMA
- `totalMultiplier: 37.7x` - сумма всех активных NFT бустов
- **Результат:** `21.00 TAMA × 37.70 = 791.70 TAMA за клик` ❌

**Проблема:** Слишком много TAMA за клик, баланс набирается слишком быстро.

---

## 💡 ВАРИАНТЫ БАЛАНСИРОВКИ

### 🎯 ВАРИАНТ 1: УМЕНЬШИТЬ BASE_CLICK_REWARD (ПРОСТОЕ РЕШЕНИЕ)

**Что изменить:**
- Сейчас: `BASE_CLICK_REWARD: 20`
- Предлагаю: `BASE_CLICK_REWARD: 1` или `2`

**Результат:**
- Базовый клик: 1-2 TAMA
- С бустом 37.7x: `2.00 × 37.70 = 75.4 TAMA` ✅ (в 10 раз меньше)

**Плюсы:**
- ✅ Простое решение
- ✅ Легко настроить через Admin Panel
- ✅ Сохраняет логику суммирования бустов

**Минусы:**
- ⚠️ Нужно изменить в Admin Panel

---

### 🎯 ВАРИАНТ 2: ИЗМЕНИТЬ ФОРМУЛУ БУСТА - ПРОЦЕНТ ВМЕСТО МУЛЬТИПЛИКАТОРА

**Текущая формула:**
```javascript
earnedTama *= (1 + userNFTBoost)  // 37.7x = ×38.7
```

**Новая формула (процент от базовой награды):**
```javascript
// userNFTBoost = сумма всех multipliers - 1
// Например: 37.7x total → userNFTBoost = 36.7
// Применяем как процент: +36.7% от базовой награды
earnedTama *= (1 + userNFTBoost * 0.1)  // 36.7 × 0.1 = 3.67x
```

**Результат:**
- Базовый клик: 20 TAMA
- С бустом: `20 × (1 + 36.7 × 0.1) = 20 × 4.67 = 93.4 TAMA` ✅

**Плюсы:**
- ✅ Сохраняет BASE_CLICK_REWARD
- ✅ Бусты дают разумный бонус
- ✅ Легко настроить коэффициент (0.1 можно изменить)

**Минусы:**
- ⚠️ Меняет логику бустов

---

### 🎯 ВАРИАНТ 3: УМЕНЬШИТЬ CAP + ЛОГАРИФМИЧЕСКАЯ ШКАЛА

**Что изменить:**
- CAP: с 100x → 10x или 20x
- Добавить логарифмическую шкалу для больших коллекций

**Формула:**
```javascript
// Суммируем все активные NFT
const totalMultiplier = activeNFTs.reduce((sum, n) => sum + multiplier, 0);

// Применяем логарифмическую шкалу
const logMultiplier = Math.log10(totalMultiplier + 1) * 10; // Например, 37.7x → ~15.8x

// Применяем CAP
const cappedMultiplier = Math.min(logMultiplier, 10.0); // CAP 10x
```

**Результат:**
- 37.7x total → ~15.8x (логарифм) → 10x (CAP)
- `20 × 10 = 200 TAMA` ✅

**Плюсы:**
- ✅ Балансирует большие коллекции
- ✅ Мотивирует собирать NFT, но не слишком мощно

**Минусы:**
- ⚠️ Сложная формула
- ⚠️ Может быть непонятно игрокам

---

### 🎯 ВАРИАНТ 4: МАКСИМАЛЬНЫЙ БУСТ + УМЕНЬШАЮЩИЙСЯ ЭФФЕКТ

**Формула:**
```javascript
// Берем максимальный буст
const maxBoost = Math.max(...activeNFTs.map(n => n.earning_multiplier));

// Добавляем уменьшающийся эффект от остальных
const otherBoosts = activeNFTs
    .map(n => n.earning_multiplier)
    .sort((a, b) => b - a)
    .slice(1) // Все кроме максимального
    .reduce((sum, mult, index) => {
        // Каждый следующий NFT дает меньше эффекта
        const diminishingFactor = 1 / (index + 2); // 50%, 33%, 25%, ...
        return sum + (mult - 1) * diminishingFactor;
    }, 0);

const totalBoost = maxBoost + otherBoosts;
```

**Пример:**
- Максимальный: 10x
- Остальные 15 NFT по 2x каждый
- Эффект: `10x + (15 × 1 × 0.5) = 10x + 7.5x = 17.5x`
- `20 × 17.5 = 350 TAMA` ✅

**Плюсы:**
- ✅ Балансирует: сильный NFT доминирует
- ✅ Коллекция все еще важна, но не слишком мощно

**Минусы:**
- ⚠️ Сложная формула
- ⚠️ Может быть несправедливо

---

### 🎯 ВАРИАНТ 5: КОМБИНАЦИЯ - УМЕНЬШИТЬ BASE + УМЕНЬШИТЬ CAP

**Что изменить:**
1. `BASE_CLICK_REWARD: 20` → `1` или `2`
2. `MAX_BOOST_CAP: 100.0` → `10.0` или `20.0`

**Результат:**
- Базовый клик: 2 TAMA
- Буст: min(37.7x, 10x) = 10x
- `2 × 10 = 20 TAMA` ✅

**Плюсы:**
- ✅ Простое решение
- ✅ Легко настроить
- ✅ Балансирует и базовую награду, и бусты

**Минусы:**
- ⚠️ Нужно изменить два параметра

---

## 🎮 РЕКОМЕНДАЦИЯ

### ✅ ВАРИАНТ 5: КОМБИНАЦИЯ (РЕКОМЕНДУЕТСЯ)

**Изменения:**
1. **BASE_CLICK_REWARD:** `20` → `2` (в 10 раз меньше)
2. **MAX_BOOST_CAP:** `100.0` → `10.0` (в 10 раз меньше)

**Результат:**
- Базовый клик: 2 TAMA
- С 10 активными NFT по 2x: `2 × 2 = 4 TAMA` (без буста)
- С бустом 10x (CAP): `2 × 10 = 20 TAMA` ✅

**Почему это лучше:**
- ✅ Простое решение
- ✅ Легко настроить через Admin Panel
- ✅ Балансирует и базовую награду, и бусты
- ✅ Сохраняет логику суммирования бустов
- ✅ Мотивирует собирать NFT, но не слишком мощно

---

## 🔧 АЛЬТЕРНАТИВНЫЙ ВАРИАНТ 2 (если хотите сохранить BASE_CLICK_REWARD)

**Изменения:**
1. **Формула буста:** `earnedTama *= (1 + userNFTBoost * 0.1)`
2. **MAX_BOOST_CAP:** `100.0` → `10.0`

**Результат:**
- Базовый клик: 20 TAMA
- С бустом 10x (CAP): `20 × (1 + 9 × 0.1) = 20 × 1.9 = 38 TAMA` ✅

---

## ❓ ВОПРОСЫ ДЛЯ РЕШЕНИЯ

1. **Какой вариант выбрать?**
   - Вариант 5 (Комбинация) - РЕКОМЕНДУЕТСЯ
   - Вариант 2 (Процент от базовой)
   - Вариант 3 (Логарифмическая шкала)
   - Ваш вариант?

2. **Какие значения установить?**
   - BASE_CLICK_REWARD: `1`, `2`, или `5`?
   - MAX_BOOST_CAP: `10x`, `20x`, или `50x`?

3. **Сохранить ли логику суммирования?**
   - Да, суммировать все активные бусты (текущая логика)
   - Или изменить на другой вариант?

