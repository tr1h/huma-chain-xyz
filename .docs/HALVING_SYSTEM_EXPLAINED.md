# 🎯 СИСТЕМА ХАЛВИНГА - ПОЛНОЕ ОБЪЯСНЕНИЕ

## ❓ ГЛАВНЫЙ ВОПРОС: "От блокчейна или виртуальных токенов?"

**ОТВЕТ:** Халвинг ограничивает **ВИРТУАЛЬНЫЕ токены** в игре, а не блокчейн! ✅

---

## 📊 КАК ЭТО РАБОТАЕТ - ПРОСТЫМИ СЛОВАМИ

### 1. **ВИРТУАЛЬНЫЕ ТОКЕНЫ (В ИГРЕ)** 💎

```
┌─────────────────────────────────────┐
│  Supabase Database (PostgreSQL)     │
│                                     │
│  👤 User: 7401131043                │
│  ├─ 💰 tama: 52,717 TAMA           │ ← ВИРТУАЛЬНЫЕ (в базе данных)
│  ├─ 📊 level: 10                    │
│  ├─ ⭐ xp: 1,250                     │
│  └─ 🎮 clicks_today: 245            │
└─────────────────────────────────────┘
```

**Что это значит:**
- ✅ Все клики по питомцу → **виртуальные токены** в Supabase
- ✅ Кормление, игра, лечение → **виртуальные токены** расходуются
- ✅ NFT минты за TAMA → **виртуальные токены** списываются
- ✅ **БЕЗ блокчейна** → быстро, без комиссий, без задержек
- ✅ **Халвинг** ограничивает сколько можно заработать в день

---

### 2. **РЕАЛЬНЫЕ ТОКЕНЫ (БЛОКЧЕЙН)** 🌐

```
┌─────────────────────────────────────┐
│  Solana Blockchain (Devnet)         │
│                                     │
│  🪙 TAMA Token (SPL)                │
│  ├─ 💸 Withdrawn: 42,750 TAMA      │ ← РЕАЛЬНЫЕ (on-chain)
│  ├─ 🔥 Burned: 1,350 TAMA           │
│  └─ 🏦 Treasury: ~40,000 TAMA       │
└─────────────────────────────────────┘
```

**Когда это происходит:**
- ✅ Пользователь нажимает **"Withdraw"** → виртуальные превращаются в реальные
- ✅ Комиссия 5% (60% сжигается 🔥, 40% в казну 🏦)
- ✅ **ТОЛЬКО** тут происходит блокчейн транзакция!
- ✅ Пользователь получает реальные SPL токены на кошелек

---

## ⏰ ХАЛВИНГ - КАК ОГРАНИЧЕНИЕ

### 🔢 Математика халвинга:

```javascript
// Код в admin-tokenomics.html
const launchDate = new Date('2025-11-01');  // Дата запуска системы
const now = new Date();
const daysSinceLaunch = Math.floor((now - launchDate) / (1000 * 60 * 60 * 24));

// Определяет период
if (daysSinceLaunch < 180) {
    // Year 1 H1 (дни 0-180)
    dailyPool = 2,222,222 TAMA  // ← Это ВИРТУАЛЬНЫЕ токены!
    totalPool = 400,000,000 TAMA
    
} else if (daysSinceLaunch < 360) {
    // Year 1 H2 (дни 181-360)
    dailyPool = 1,111,111 TAMA  // ← ХАЛВИНГ! В 2 раза меньше!
    totalPool = 200,000,000 TAMA
    
} else if (daysSinceLaunch < 540) {
    // Year 2 H1 (дни 361-540)
    dailyPool = 555,555 TAMA    // ← Еще халвинг!
    totalPool = 100,000,000 TAMA
}
```

---

### 🎯 Что это ограничивает:

| ✅ ОГРАНИЧИВАЕТ | ❌ НЕ ОГРАНИЧИВАЕТ |
|----------------|-------------------|
| Виртуальные токены в игре | Блокчейн токены |
| Заработок от кликов | Количество кликов |
| Daily Quests награды | Выводы на кошелек |
| Referral бонусы | NFT минты за SOL |
| Achievement награды | Торговлю токенами |

**Цель:** Ограничить инфляцию виртуальных токенов и сделать их ценнее со временем!

---

## 🔍 КАК ПРОВЕРИТЬ ЧТО ВСЕ РАБОТАЕТ?

### 1. **Через Admin Panel**

Открой: https://tr1h.github.io/huma-chain-xyz/admin-tokenomics.html

**Что проверять:**
```
📊 TOTAL SUPPLY: 1,000,000,000 TAMA
   ↳ Это максимум (не меняется)

💫 CIRCULATING SUPPLY: 42,750 TAMA
   ↳ Сколько реально выведено на кошельки (БЛОКЧЕЙН)

🔥 BURNED: 1,350 TAMA
   ↳ Сколько сожжено (60% от комиссий при выводе)

💧 DAILY POOL: 2,222,222 TAMA
   ↳ Сколько можно заработать сегодня (ВИРТУАЛЬНЫЕ)

⏰ HALVING COUNTDOWN: 180 days left
   ↳ Когда произойдет следующий халвинг
   
🎨 NFT MINTS: 3 NFTs / 7,500 TAMA
   ↳ Сколько NFT куплено за TAMA (ВИРТУАЛЬНЫЕ)
   
🌐 BLOCKCHAIN TRANSACTIONS:
   ↳ P2E Pool Refunds: +750 TAMA (возврат в пул от NFT)
   ↳ Treasury Income: +300 TAMA (40% комиссий)
   ↳ Burned: 450 TAMA (60% комиссий)
```

---

### 2. **Проверка через Console**

Открой игру и нажми **F12** → **Console**:

```javascript
// Посмотреть текущий период
fetch('https://huma-chain-xyz.onrender.com/api/tama/economy/active')
    .then(r => r.json())
    .then(data => console.log('💰 Economy:', data));

// Посмотреть свой баланс
// (замени 7401131043 на свой Telegram ID)
fetch('https://huma-chain-xyz.onrender.com/api/tama/leaderboard/upsert', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({user_id: '7401131043'})
})
    .then(r => r.json())
    .then(data => console.log('👤 My balance:', data));
```

---

### 3. **Проверка халвинга вручную**

**Сейчас (2025-11-09):**
```
Дата запуска: 2025-11-01
Прошло дней: 8 дней
Период: Year 1 H1 (день 8 из 180)
Daily Pool: 2,222,222 TAMA
До халвинга: 172 дня
```

**После 2025-04-30 (через 172 дня):**
```
Период: Year 1 H2 (день 181 из 360)
Daily Pool: 1,111,111 TAMA  ← ХАЛВИНГ!
До халвинга: 180 дней
```

**Как проверить после халвинга:**
1. Открой admin-tokenomics.html
2. Смотри "💧 DAILY POOL"
3. Должно быть **1,111,111 TAMA** (в 2 раза меньше)
4. Прогресс-бар должен быть на 0%

---

## 🎮 ПРИМЕРЫ ИЗ ЖИЗНИ

### Пример 1: Пользователь играет в игру

```
1. User кликает по питомцу 100 раз
   → +20 TAMA за клик (BASE_CLICK_REWARD из Admin Panel)
   → +2,000 TAMA (ВИРТУАЛЬНЫЕ в Supabase)
   ↳ Баланс: 52,717 → 54,717 TAMA

2. User покупает NFT за 2,500 TAMA
   → -2,500 TAMA (ВИРТУАЛЬНЫЕ списываются)
   → +750 TAMA возвращается в P2E Pool (БЛОКЧЕЙН)
   → +1,000 TAMA идет в Treasury (БЛОКЧЕЙН)
   → +750 TAMA виртуально сжигается
   ↳ Баланс: 54,717 → 52,217 TAMA

3. User выводит 10,000 TAMA на кошелек
   → -10,000 TAMA (ВИРТУАЛЬНЫЕ списываются)
   → -500 TAMA комиссия (5%)
   → +9,500 TAMA реально приходит на кошелек (БЛОКЧЕЙН)
   → 300 TAMA (60% от 500) виртуально сжигается
   → 200 TAMA (40% от 500) идет в Treasury (БЛОКЧЕЙН)
   ↳ Баланс: 52,217 → 42,217 TAMA (виртуальные)
   ↳ Кошелек: 0 → 9,500 TAMA (реальные)
```

---

### Пример 2: Халвинг в действии

**Сегодня (Day 8, Period Year 1 H1):**
```
User кликает 1,000 раз в день
→ BASE_CLICK_REWARD = 20 TAMA
→ Заработок: 20,000 TAMA в день (теоретически)
→ Ограничение Daily Pool: 2,222,222 TAMA
→ User может заработать максимум свою долю от Daily Pool
```

**После халвинга (Day 181, Period Year 1 H2):**
```
User кликает 1,000 раз в день
→ BASE_CLICK_REWARD = 20 TAMA (не меняется!)
→ Заработок: 20,000 TAMA в день (теоретически)
→ Ограничение Daily Pool: 1,111,111 TAMA  ← ХАЛВИНГ!
→ User получит меньше, т.к. Daily Pool уменьшился
```

---

## 📋 ИТОГОВАЯ ТАБЛИЦА

| Параметр | Тип | Где хранится | Халвинг влияет? |
|----------|-----|--------------|----------------|
| 💰 TAMA баланс в игре | Виртуальные | Supabase `leaderboard.tama` | ✅ Да (ограничен Daily Pool) |
| 🪙 TAMA на кошельке | Реальные | Solana Blockchain (SPL) | ❌ Нет (уже выведены) |
| 🎨 NFT | Реальные | Solana Blockchain (Metaplex) | ❌ Нет (уже созданы) |
| 💧 Daily Pool | Виртуальные | Математическая формула | ✅ Да (уменьшается каждые 180 дней) |
| 🔥 Burned | Виртуальные | Supabase `transactions` | ❌ Нет (просто статистика) |
| 🏦 Treasury | Реальные | Solana Blockchain | ❌ Нет (уже на кошельке) |

---

## ✅ ЗАКЛЮЧЕНИЕ

### Система работает так:

1. **В игре** → все ВИРТУАЛЬНЫЕ токены в базе данных
2. **Халвинг** → ограничивает сколько виртуальных токенов можно заработать
3. **Withdraw** → виртуальные превращаются в реальные (блокчейн)
4. **После вывода** → это уже реальные токены, халвинг на них не влияет

### Почему так сделано:

- ✅ **Быстро**: Нет задержек блокчейна при клике
- ✅ **Дешево**: Нет комиссий за каждый клик
- ✅ **Масштабируемо**: Миллионы кликов без перегрузки блокчейна
- ✅ **Контролируемо**: Халвинг ограничивает инфляцию
- ✅ **Честно**: Все видно в admin-tokenomics.html

---

**🎯 Это стандартная архитектура для P2E игр!**

Примеры:
- Axie Infinity → SLP токены виртуальные, потом выводятся
- StepN → GST токены виртуальные, потом выводятся
- Splinterlands → DEC токены виртуальные, потом выводятся

**Все делают так же! ✅**

