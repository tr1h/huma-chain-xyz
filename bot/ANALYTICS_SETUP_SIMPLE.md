# 📊 Автоматическая Analytics для Telegram Бота - Простая Инструкция

**Создано:** @Developer  
**Для:** Админа проекта  
**Время установки:** 10 минут

---

## 🎯 ЧТО ЭТО ДАЁТ

### БЕЗ ANALYTICS (было раньше):
- ❌ Нужно вручную заполнять SQL запросы
- ❌ Нужно открывать Supabase
- ❌ Нужно копировать данные в отчёты
- ❌ Не видно проблем до поздна
- ❌ Нет автоматических уведомлений

### С ANALYTICS (теперь):
- ✅ **Всё автоматически!**
- ✅ Отчёты приходят в Telegram
- ✅ Алерты при проблемах
- ✅ Никакого ручного труда
- ✅ Всегда актуальные данные

---

## 🚀 БЫСТРАЯ УСТАНОВКА (3 ШАГА)

### Шаг 1: Скопировать файлы

Уже готово! Файлы созданы:
```
bot/
├── analytics.py                         ✅ Готово!
└── bot_analytics_integration.py         ✅ Готово!
```

### Шаг 2: Добавить код в bot.py

Открой `bot/bot.py` и добавь 3 блока кода:

#### 2.1 В начале файла (после других imports):

```python
# Import analytics system
try:
    from analytics import (
        init_analytics,
        format_daily_report,
        format_weekly_report,
        format_quick_health,
        check_critical_alerts
    )
    ANALYTICS_ENABLED = True
    print("✅ Analytics system enabled")
except Exception as e:
    ANALYTICS_ENABLED = False
    print(f"⚠️ Analytics system disabled: {e}")
```

#### 2.2 После создания supabase client (найди строку `supabase = create_client(...)`):

```python
# Initialize Analytics
analytics_collector = None
if ANALYTICS_ENABLED:
    try:
        analytics_collector = init_analytics(SUPABASE_URL, SUPABASE_KEY)
    except Exception as e:
        print(f"⚠️ Failed to initialize analytics: {e}")
        ANALYTICS_ENABLED = False
```

#### 2.3 Скопируй ВСЕ команды

Открой файл `bot/bot_analytics_integration.py` и скопируй:
- Все команды из `ADMIN_COMMANDS_CODE`
- Scheduled jobs из `SCHEDULED_JOBS_CODE`

Вставь в bot.py в секцию ADMIN COMMANDS (где уже есть `/monitor`, `/broadcast` и т.д.)

### Шаг 3: Перезапустить бота

```bash
cd bot
python bot.py
```

Если видишь:
```
✅ Analytics system enabled
✅ Analytics connected to Supabase
✅ Analytics scheduled jobs configured
```

**ВСЁ РАБОТАЕТ! 🎉**

---

## 📱 КАК ПОЛЬЗОВАТЬСЯ

### В Telegram боте отправь команды:

```
/analytics       - Меню всех команд
/health          - Быстрый статус (30 сек)
/daily_report    - Полный ежедневный отчёт
/weekly_report   - Еженедельный + топ игроков
/economy         - Детали TAMA экономики
/players         - Статистика игроков
/nfts            - NFT продажи и revenue
/burnmint        - Burn/Mint tracking
```

### Примеры:

#### `/health` - Быстрый check:
```
🎯 QUICK HEALTH CHECK
Score: 87/100 🟢 HEALTHY

✅ All systems operational!

Use /analytics for full report
```

#### `/daily_report` - Полный отчёт:
```
📊 DAILY ANALYTICS REPORT
📅 2025-12-18 20:55

🎯 HEALTH SCORE: 87/100
Status: 🟢 HEALTHY

━━━━━━━━━━━━━━━━━━━

👥 PLAYERS
• Total: 156
• New (24h): +12
• New (7d): +47
• Avg Level: 5.2

📈 ACTIVITY
• DAU: 45
• WAU: 98
• MAU: 145

💰 ECONOMY
• Burn/Mint: 0.92 🟢
• Burned (7d): 45,000 TAMA
• Minted (7d): 48,900 TAMA

... и так далее
```

---

## 🔔 АВТОМАТИЧЕСКИЕ УВЕДОМЛЕНИЯ

### Ежедневный отчёт:
- **Когда:** Каждый день в 9:00
- **Что:** Полная статистика за 24 часа
- **Кому:** Всем админам

### Еженедельный отчёт:
- **Когда:** Каждый понедельник в 10:00
- **Что:** Недельная статистика + топ-10 игроков
- **Кому:** Всем админам

### Критические алерты:
- **Когда:** Каждый час проверка
- **Что:** Если burn/mint < 0.5, retention < 20%, нет новых игроков
- **Кому:** Всем админам немедленно

**Пример алерта:**
```
🚨 CRITICAL ALERT

🚨 CRITICAL: Burn/Mint ratio extremely low: 0.32
🚨 CRITICAL: Day 1 retention very low: 18%

Take action immediately!
```

---

## ⚙️ НАСТРОЙКА РАСПИСАНИЯ

Если хочешь изменить время отчётов, отредактируй в `bot.py`:

```python
# Ежедневный отчёт
schedule.every().day.at("09:00").do(send_daily_analytics)
#                        ^^^^^
#                        Измени время здесь

# Еженедельный отчёт
schedule.every().monday.at("10:00").do(send_weekly_analytics)
#                ^^^^^^    ^^^^^^
#                День      Время

# Проверка алертов
schedule.every().hour.do(check_and_alert)
#                ^^^^
#                Можно изменить на .minutes, .hours и т.д.
```

---

## 🧪 ТЕСТИРОВАНИЕ

### После установки протестируй:

```
/test_analytics  - Проверить все компоненты
```

Увидишь:
```
📊 ANALYTICS TEST RESULTS

✅ Player stats: 156 users
✅ Active users: DAU=45
✅ Economy: 1,234,567 TAMA
✅ NFTs: 89 sold
✅ Burn/Mint: 0.92 🟢

✅ All tests completed!
```

### Если что-то не работает:

#### Ошибка "Analytics not available":
- Проверь что добавил imports
- Проверь что `ANALYTICS_ENABLED = True`
- Проверь логи бота при запуске

#### Ошибка "Failed to connect to Supabase":
- Проверь .env файл
- Проверь `SUPABASE_URL` и `SUPABASE_KEY`
- Проверь доступ к интернету

#### Ошибка в SQL запросах:
- Проверь что таблицы существуют: `users`, `transactions`, `user_nfts`
- Проверь что RLS policies не блокируют запросы
- Используй Service Role Key для полного доступа

---

## 📊 ЧТО ОТСЛЕЖИВАЕТСЯ

### 📈 Метрики:

| Категория | Метрики |
|-----------|---------|
| **Players** | Total, New 24h/7d, Avg Level, Max Level |
| **Activity** | DAU, WAU, MAU, Stickiness |
| **Economy** | Circulating TAMA, Avg Balance, Top 10 Concentration |
| **Burn/Mint** | Burned/Minted, Ratio, Status |
| **NFTs** | Total Sold, This Week, By Tier, Conversion |
| **Revenue** | SOL Earned, USD Equiv |
| **Retention** | Day 1 Retention |

### 🎯 Health Score (0-100):

Считается из 4 факторов:
1. **Player Growth** (25 pts) - новые игроки
2. **Burn/Mint Ratio** (25 pts) - дефляция
3. **Revenue** (25 pts) - NFT продажи
4. **Retention** (25 pts) - возврат игроков

**Оценки:**
- 🟢 80-100: Excellent
- 🟡 60-79: Good
- 🔴 <60: Needs attention

---

## 🆘 TROUBLESHOOTING

### Проблема: Бот не запускается

**Решение:**
```bash
# Проверь syntax errors
python -m py_compile bot/analytics.py

# Проверь imports
python -c "from analytics import init_analytics; print('OK')"
```

### Проблема: Analytics says "not available"

**Решение:**
1. Проверь что файл `analytics.py` в папке `bot/`
2. Проверь что добавил imports в `bot.py`
3. Проверь логи: `print(ANALYTICS_ENABLED)` должно быть `True`

### Проблема: Нет автоматических отчётов

**Решение:**
1. Проверь что schedule jobs добавлены
2. Проверь что бот работает 24/7 (не останавливается)
3. Проверь время: `datetime.now()` должно совпадать с твоей timezone

### Проблема: Пустые данные в отчётах

**Решение:**
1. Проверь Supabase: есть ли данные в таблицах?
2. Проверь RLS policies: может блокируют чтение
3. Используй Service Role Key вместо Anon Key

---

## 💡 PRO TIPS

### 1. Добавь ещё админов

В `.env` файле:
```bash
ADMIN_IDS=123456789,987654321,111222333
#          ^^^^^^^^^  ^^^^^^^^^  ^^^^^^^^^
#          Admin 1    Admin 2    Admin 3
```

Все получат отчёты!

### 2. Измени порог алертов

В `analytics.py` найди функцию `check_critical_alerts()`:
```python
# Изменить порог burn/mint
if burn_mint['ratio'] < 0.3:  # Было 0.3, можно изменить
    alerts.append(...)

# Изменить порог retention
if retention['day1_retention'] < 20:  # Было 20, можно изменить
    alerts.append(...)
```

### 3. Добавь свои метрики

В `analytics.py` добавь новую функцию:
```python
def get_my_custom_metric(self) -> Dict[str, Any]:
    """Твоя кастомная метрика"""
    try:
        # Твой SQL запрос
        data = self.supabase.table('my_table')\
            .select('*')\
            .execute()
        
        return {'my_value': len(data.data)}
    except Exception as e:
        logger.error(f"Error: {e}")
        return {'my_value': 0}
```

Потом используй в командах!

### 4. Export в CSV

Добавь команду в `bot.py`:
```python
@bot.message_handler(commands=['export_csv'])
def export_analytics_csv(message):
    if not is_admin(message.from_user.id):
        return
    
    # Собрать данные
    health = analytics_collector.get_health_score()
    
    # Конвертировать в CSV
    import csv, io
    output = io.StringIO()
    writer = csv.writer(output)
    
    writer.writerow(['Metric', 'Value'])
    writer.writerow(['Score', health['score']])
    # ... добавить все метрики
    
    # Отправить как файл
    output.seek(0)
    bot.send_document(message.chat.id, 
        ('analytics.csv', output.getvalue().encode()))
```

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

### Полные отчёты (если нужны):

Все отчёты из @Analytics всё ещё доступны:
- `docs/reports/MASTER_DASHBOARD_2025-12-18.md`
- `docs/reports/ANALYTICS_COMPREHENSIVE_REPORT_2025-12-18.md`
- `docs/reports/WEEKLY_ECONOMY_HEALTH_2025-W51.md`
- И другие...

Но теперь ты получаешь всё автоматически в Telegram! 🎉

### SQL Запросы:

Если хочешь понять как работает analytics, смотри функции в `analytics.py`:
- `get_player_stats()` - статистика игроков
- `get_tama_economy()` - экономика TAMA
- `get_burn_mint_ratio()` - burn/mint tracking
- И т.д.

Все запросы оптимизированы и работают быстро!

---

## ✅ CHECKLIST УСТАНОВКИ

Перед использованием проверь:

- [ ] Файл `analytics.py` в папке `bot/`
- [ ] Imports добавлены в `bot.py`
- [ ] Analytics инициализирован после supabase client
- [ ] Команды добавлены в ADMIN COMMANDS
- [ ] Scheduled jobs добавлены перед `bot.polling()`
- [ ] Бот перезапущен
- [ ] Команда `/test_analytics` работает
- [ ] Команда `/health` показывает данные
- [ ] ADMIN_IDS правильно настроены в .env

**Всё ✅?** → Готово к использованию! 🚀

---

## 🎉 РЕЗУЛЬТАТ

### До:
```
Админ: "Как там экономика?"
Разработчик: "Сейчас открою Supabase..."
*15 минут спустя*
Разработчик: "Вот таблица с данными..."
```

### После:
```
Админ: /health
Бот: Score 87/100 🟢 HEALTHY
     ✅ All systems operational!
```

**PROFIT!** 💰

---

## 🙏 СПАСИБО

Создано: @Developer  
По запросу: @Analytics  
Для: Админа проекта  

**Enjoy automated analytics!** 📊🚀

---

## 📞 SUPPORT

Проблемы? Вопросы?

1. Проверь этот файл ещё раз
2. Посмотри логи бота
3. Проверь что Supabase доступен
4. Спроси @Developer в чате

**Good luck!** 🍀
