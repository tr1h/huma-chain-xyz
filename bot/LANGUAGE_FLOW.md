# 🌍 Как работает выбор языка (визуально)

## 📊 Схема определения языка:

```
┌─────────────────────────────────────────┐
│   Пользователь отправляет команду       │
│   (например: /start)                    │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│   1. Проверяем БД: есть выбор языка?    │
└───────────────┬─────────────────────────┘
                │
        ┌───────┴───────┐
        │               │
        ▼ ДА            ▼ НЕТ
┌───────────────┐   ┌────────────────────────┐
│ Используем    │   │ 2. Проверяем Telegram  │
│ сохранённый   │   │    language_code       │
│ язык из БД    │   └──────────┬─────────────┘
└───────┬───────┘              │
        │              ┌───────┴───────┐
        │              │               │
        │              ▼ ru/uk/be      ▼ другое
        │      ┌──────────────┐   ┌─────────────────┐
        │      │ Используем   │   │ 3. Анализируем  │
        │      │ русский      │   │    текст        │
        │      └──────┬───────┘   └────────┬────────┘
        │             │                    │
        │             │            ┌───────┴────────┐
        │             │            │                │
        │             │            ▼ Кириллица     ▼ Нет
        │             │      ┌──────────┐   ┌──────────┐
        │             │      │ Русский  │   │ English  │
        │             │      └────┬─────┘   └────┬─────┘
        │             │           │              │
        └─────────────┴───────────┴──────────────┘
                      │
                      ▼
        ┌──────────────────────────────┐
        │ Отправляем сообщение на      │
        │ определённом языке           │
        └──────────────────────────────┘
```

---

## 🔄 UX Flow: Первый запуск

```
┌─────────────────────────────────────────────────────┐
│ Пользователь: /start                                │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│ Бот проверяет:                                      │
│ ✓ БД: preferred_language = NULL                     │
│ ✓ Telegram: language_code = "ru"                    │
│ → Определяет язык: RU                               │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│ Бот отправляет:                                     │
│                                                     │
│ 🎮 Добро пожаловать в Solana Tamagotchi!           │
│ Помнишь детство? Теперь с заработком! 🐾           │
│                                                     │
│ [Полный текст на русском]                          │
│                                                     │
│ 💡 Ты можешь изменить язык командой /language       │
└─────────────────────────────────────────────────────┘
```

---

## 🔄 UX Flow: Смена языка

```
┌─────────────────────────────────────────────────────┐
│ Пользователь: /language                             │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│ Бот показывает:                                     │
│                                                     │
│ 🌍 Choose Your Language / Выбери язык               │
│ Select your preferred language:                     │
│                                                     │
│ ┌──────────────┐  ┌──────────────┐                 │
│ │ 🇬🇧 English  │  │ 🇷🇺 Русский  │                 │
│ └──────────────┘  └──────────────┘                 │
└─────────────────────────────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
┌──────────────┐    ┌──────────────┐
│ Нажал EN     │    │ Нажал RU     │
└──────┬───────┘    └──────┬───────┘
       │                   │
       ▼                   ▼
┌──────────────────┐  ┌──────────────────┐
│ Сохраняем в БД:  │  │ Сохраняем в БД:  │
│ preferred_       │  │ preferred_       │
│ language = 'en'  │  │ language = 'ru'  │
└──────┬───────────┘  └──────┬───────────┘
       │                     │
       └──────────┬──────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│ Бот подтверждает:                                   │
│                                                     │
│ ✅ Language changed to English!                     │
│ (или)                                               │
│ ✅ Язык изменён на русский!                         │
│                                                     │
│ [Показывает /start на выбранном языке]              │
└─────────────────────────────────────────────────────┘
```

---

## 🗄️ Структура данных в БД

```
┌─────────────────────────────────────────────────────────┐
│ Таблица: telegram_users                                 │
├──────────────┬────────────────┬─────────────────────────┤
│ Колонка      │ Тип            │ Описание                │
├──────────────┼────────────────┼─────────────────────────┤
│ telegram_id  │ BIGINT         │ Telegram User ID (PK)   │
│ username     │ VARCHAR        │ Telegram username       │
│ tama_balance │ INTEGER        │ TAMA баланс             │
│ level        │ INTEGER        │ Уровень пользователя    │
│ xp           │ INTEGER        │ Опыт                    │
│ ...          │ ...            │ ...                     │
│              │                │                         │
│ ↓ НОВАЯ КОЛОНКА ↓                                       │
├──────────────┼────────────────┼─────────────────────────┤
│ preferred_   │ VARCHAR(2)     │ 'en', 'ru' или NULL     │
│ language     │ DEFAULT NULL   │ NULL = не выбран        │
└──────────────┴────────────────┴─────────────────────────┘

Примеры:
┌─────────────┬──────────┬──────────┬──────┬─────────────────┐
│ telegram_id │ username │ tama_bal │ lvl  │ preferred_lang  │
├─────────────┼──────────┼──────────┼──────┼─────────────────┤
│ 2139640084  │ gotchi   │ 18283    │ 8    │ 'ru'           │
│ 202140267   │ user123  │ 5420     │ 3    │ 'en'           │
│ 999888777   │ newbie   │ 0        │ 1    │ NULL           │
└─────────────┴──────────┴──────────┴──────┴─────────────────┘
                                             ↑
                                    Не выбран - используем
                                    автоопределение
```

---

## 💻 Примеры кода (упрощённо)

### Определение языка:

```python
def get_user_lang(user_id: int, telegram_lang: str, message_text: str) -> str:
    # 1. Проверяем БД
    saved_lang = db.get_user_language(user_id)
    if saved_lang:
        return saved_lang  # 'en' или 'ru'
    
    # 2. Проверяем Telegram
    if telegram_lang in ['ru', 'uk', 'be', 'kk']:  # СНГ языки
        return 'ru'
    
    # 3. Анализируем текст
    if has_cyrillic(message_text):
        return 'ru'
    
    # 4. По умолчанию
    return 'en'
```

### Отправка сообщения:

```python
@bot.message_handler(commands=['start'])
def start(message):
    user_id = message.from_user.id
    
    # Определяем язык
    lang = get_user_lang(
        user_id=user_id,
        telegram_lang=message.from_user.language_code,
        message_text=message.text
    )
    
    # Получаем перевод
    text = translations['start'][lang]
    
    # Отправляем
    bot.send_message(user_id, text)
```

### Смена языка:

```python
@bot.message_handler(commands=['language'])
def choose_language(message):
    # Показываем кнопки
    keyboard = InlineKeyboardMarkup()
    keyboard.add(
        InlineKeyboardButton("🇬🇧 English", callback_data="lang_en"),
        InlineKeyboardButton("🇷🇺 Русский", callback_data="lang_ru")
    )
    bot.send_message(message.chat.id, "🌍 Choose / Выбери:", reply_markup=keyboard)


@bot.callback_query_handler(func=lambda c: c.data.startswith('lang_'))
def save_language(call):
    lang = call.data.replace('lang_', '')  # 'en' или 'ru'
    user_id = call.from_user.id
    
    # Сохраняем в БД
    db.save_user_language(user_id, lang)
    
    # Подтверждаем
    bot.answer_callback_query(call.id, "✅")
    bot.send_message(user_id, translations['language_changed'][lang])
```

---

## 🎯 Статистика (прогноз)

### До внедрения:
```
┌──────────────────────────────────────┐
│ Язык интерфейса: ТОЛЬКО English      │
├──────────────────────────────────────┤
│ Пользователей из России: 60%         │
│ Понимают всё: 30%                    │
│ Не понимают: 70% ❌                  │
│                                      │
│ DAU: 5-10                            │
│ Retention D7: 30%                    │
│ Avg session: 5 min                   │
└──────────────────────────────────────┘
```

### После внедрения:
```
┌──────────────────────────────────────┐
│ Язык интерфейса: EN + RU (авто)      │
├──────────────────────────────────────┤
│ Выбрали Русский: 65%                 │
│ Выбрали English: 35%                 │
│ Понимают всё: 95% ✅                 │
│                                      │
│ DAU: 20-30 (+200%)                   │
│ Retention D7: 50% (+20%)             │
│ Avg session: 10 min (+100%)          │
└──────────────────────────────────────┘
```

---

## ✅ Итого: 3 уровня + UX

**Уровень 1:** Выбор пользователя (команда `/language`)  
**Уровень 2:** Telegram язык (автоматически)  
**Уровень 3:** Анализ текста (кириллица)  

**Результат:** Твой брат (11 лет) поймёт ВСЁ! 🎉

**Время внедрения:** 30-40 минут  
**Сложность:** 🟢 Легко  
**Эффект:** 🚀 Огромный  

---

## 🚀 Следующие шаги:

1. Добавить колонку в БД (SQL выше)
2. Интегрировать код в `bot.py`
3. Тестировать с братом
4. 🎉 Profit!

**Готов начать?** 💪

