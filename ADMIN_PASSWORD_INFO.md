# 🔐 ADMIN PASSWORD INFORMATION

## 📝 **ПАРОЛЬ ОТ ВСЕХ АДМИНОК:**

```
SoL@N@T@m@9otch!2025_SecureAdm1n
```

**Источник:** `admin-password.js` (строка 9)

---

## 🌐 **ГДЕ РАБОТАЕТ:**

Этот пароль работает на ВСЕХ админских страницах:

1. ✅ **super-admin.html** - главная админка
2. ✅ **treasury-monitor.html** - treasury monitor
3. ✅ **admin-tokenomics.html** - токеномика
4. ✅ **admin-dashboard.html** - просмотр таблиц
5. ✅ **admin-table.html** - audit tool
6. ✅ **admin-referrals.html** - referral management
7. ✅ **admin-nft-tiers.html** - NFT tiers настройки
8. ✅ **economy-admin.html** - экономика
9. ✅ **treasury-monitor.html** - казна
10. ✅ **blog-admin.html** - блог
11. ✅ **Все остальные admin-*.html**

---

## 🔒 **КАК РАБОТАЕТ АВТОРИЗАЦИЯ:**

### **1. Локальная разработка:**
- Файл: `admin-password.js`
- Пароль: открытый текст
- Используется для разработки

### **2. Продакшен:**
- Meta tag в HTML: `<meta name="admin-password-hash" content="...">`
- Хэш: SHA-256
- Текущий хэш: `86303a8eab5693506714a32875b556fd84daf2209f184459642af6e153da3ff3`

### **3. Сессия:**
- Хранится в `sessionStorage`
- Время жизни: 30 минут
- После закрытия браузера - сбрасывается

---

## ⚠️ **ВАЖНО ДЛЯ БЕЗОПАСНОСТИ:**

### **1. НЕ КОММИТИТЬ В GIT!**
Файл `admin-password.js` должен быть в `.gitignore`:
```
# Admin passwords
admin-password.js
```

### **2. СМЕНИ ПАРОЛЬ ПЕРЕД МАЙННЕТОМ!**
**Как сгенерировать новый хэш:**
```bash
# В Linux/Mac:
echo -n "ТВОЙ_НОВЫЙ_ПАРОЛЬ" | sha256sum

# В PowerShell (Windows):
$password = "ТВОЙ_НОВЫЙ_ПАРОЛЬ"
$hash = [System.Security.Cryptography.SHA256]::Create()
$bytes = [System.Text.Encoding]::UTF8.GetBytes($password)
$hashBytes = $hash.ComputeHash($bytes)
($hashBytes | ForEach-Object { $_.ToString("x2") }) -join ""
```

**Замени в:**
- Все `admin-*.html` файлы → мета-тег `admin-password-hash`
- `admin-password.js` → переменная `ADMIN_PASSWORD`

### **3. ДОПОЛНИТЕЛЬНАЯ БЕЗОПАСНОСТЬ:**
```javascript
// В admin-auth.js:
const MAX_FAILED_ATTEMPTS = 5;        // 5 попыток
const LOCKOUT_TIME = 30 * 1000;       // 30 секунд блокировки
const SESSION_TIMEOUT = 30 * 60 * 1000; // 30 минут сессия
```

---

## 🔧 **КАК СМЕНИТЬ ПАРОЛЬ:**

### **Шаг 1: Придумай новый пароль**
Например: `MyN3wS3cur3P@ssw0rd!2025`

### **Шаг 2: Сгенерируй SHA-256 хэш**
```powershell
$password = "MyN3wS3cur3P@ssw0rd!2025"
$hash = [System.Security.Cryptography.SHA256]::Create()
$bytes = [System.Text.Encoding]::UTF8.GetBytes($password)
$hashBytes = $hash.ComputeHash($bytes)
$newHash = ($hashBytes | ForEach-Object { $_.ToString("x2") }) -join ""
Write-Host "Новый хэш: $newHash"
```

### **Шаг 3: Замени везде**
**В файле `admin-password.js`:**
```javascript
window.ADMIN_PASSWORD = 'MyN3wS3cur3P@ssw0rd!2025'; // новый пароль
```

**Во ВСЕХ admin-*.html файлах:**
```html
<meta name="admin-password-hash" content="[НОВЫЙ_ХЭШ]">
```

### **Шаг 4: Проверь**
1. Открой любую админку
2. Введи новый пароль
3. Должно войти ✅

---

## 📊 **ЛОГИРОВАНИЕ:**

Все попытки входа логируются в `localStorage`:
```javascript
// Посмотреть логи в консоли браузера:
JSON.parse(localStorage.getItem('admin_access_log') || '[]')
```

**Что логируется:**
- ✅ Успешные входы
- ❌ Неудачные попытки
- ⏱️ Время сессий
- 🔒 Блокировки (после 5 попыток)
- 📄 Страница доступа

---

## 🎯 **РЕКОМЕНДАЦИИ ДЛЯ МАЙННЕТА:**

### **1. Смени пароль:**
- Минимум 16 символов
- Большие и малые буквы
- Цифры и спецсимволы
- НЕ используй "tamagotchi", "admin", "password"

### **2. Добавь IP Whitelist:**
```php
// В начале каждой admin-*.html или в PHP файле:
$allowedIPs = ['YOUR_IP', 'ANOTHER_IP'];
if (!in_array($_SERVER['REMOTE_ADDR'], $allowedIPs)) {
    die('Access denied');
}
```

### **3. Используй HTTPS:**
- ✅ Все админки только через HTTPS
- ❌ HTTP запрещен

### **4. 2FA (опционально):**
- Google Authenticator
- SMS коды
- Email confirmation

---

## 🔑 **БЫСТРЫЙ ДОСТУП:**

**URL админок:**
- https://solanatamagotchi.com/super-admin.html
- https://solanatamagotchi.com/treasury-monitor.html
- https://solanatamagotchi.com/admin-tokenomics.html
- И т.д.

**Пароль:**
```
SoL@N@T@m@9otch!2025_SecureAdm1n
```

**Копировать:** Выдели → Ctrl+C → Вставь в поле пароля

---

## ⚠️ **ВНИМАНИЕ:**

1. ❌ НЕ храни этот файл в Git
2. ❌ НЕ делись паролем
3. ✅ Меняй пароль каждые 3-6 месяцев
4. ✅ Используй менеджер паролей (LastPass, 1Password)
5. ✅ Включи 2FA на домене (Cloudflare, Render.com)

---

**Создано:** 4 декабря 2025
**Для проекта:** Solana Tamagotchi
**Статус:** 🔐 Конфиденциально

