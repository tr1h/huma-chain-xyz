# 🛠️ Рекомендуемые расширения Cursor/VS Code для Solana Tamagotchi

## 📊 **Текущие метрики проекта:**
- ✅ **63 игрока** (рост!)
- ✅ **974,949 TAMA** заработано
- ✅ **67 NFT holders**
- ✅ **550 визитов** на сайт
- ✅ **329 уникальных посетителей**

---

## 🎯 **Критически важные расширения:**

### 1. **Web Development Essentials**
```json
"dbaeumer.vscode-eslint"          // Линтер для JavaScript
"esbenp.prettier-vscode"           // Форматирование кода
"bradlc.vscode-tailwindcss"        // Tailwind CSS поддержка
```

### 2. **Python (для Telegram бота)**
```json
"ms-python.python"                 // Python поддержка
"ms-python.vscode-pylance"         // Python IntelliSense
"ms-python.black-formatter"        // Форматирование Python
```

### 3. **PHP (для API)**
```json
"bmewburn.vscode-intelephense-client"  // PHP IntelliSense
"DEVSENSE.phptools-vscode"             // PHP Tools
```

### 4. **Solana Development** 🔥
```json
"solana.solana-dev"                // Solana разработка
```

### 5. **Database & Supabase**
```json
"mtxr.sqltools"                    // SQL Tools
"mtxr.sqltools-driver-pg"          // PostgreSQL драйвер
"cweijan.vscode-database-client2"  // Подключение к БД
```

---

## 🚀 **Полезные для продуктивности:**

### **Git & Version Control**
- `eamodio.gitlens` - Расширенная Git интеграция
- `mhutchie.git-graph` - Визуализация Git истории

### **Code Quality**
- `streetsidesoftware.code-spell-checker` - Проверка орфографии
- `usernamehw.errorlens` - Показ ошибок прямо в коде
- `gruntfuggly.todo-tree` - Поиск TODO комментариев

### **HTML/CSS/JS**
- `formulahendry.auto-rename-tag` - Автопереименование тегов
- `zignd.html-css-class-completion` - Автодополнение классов
- `ecmel.vscode-html-css` - HTML/CSS поддержка

### **Data & JSON**
- `mechatroner.rainbow-csv` - Подсветка CSV (для аналитики)
- `zainchen.json` - JSON форматирование

---

## 📦 **Как установить:**

### **Автоматически:**
1. Откройте Cursor
2. Нажмите `Ctrl+Shift+X` (Extensions)
3. Cursor предложит установить рекомендованные расширения из `.vscode/extensions.json`

### **Вручную:**
1. Откройте Command Palette (`Ctrl+Shift+P`)
2. Введите `Extensions: Show Recommended Extensions`
3. Установите нужные расширения

---

## 🎯 **Специально для вашего проекта:**

### **1. Solana Development**
```bash
# Установите Solana CLI (если еще не установлен)
sh -c "$(curl -sSfL https://release.solana.com/stable/install)"
```

### **2. Database Client**
- Подключитесь к Supabase через `cweijan.vscode-database-client2`
- Используйте connection string из вашего `.env`

### **3. GitLens для отслеживания изменений**
- Полезно для отслеживания метрик в админке
- Видно кто и когда менял код

### **4. Rainbow CSV**
- Отлично для анализа данных из Supabase
- Экспорт данных в CSV и анализ

---

## 🔥 **Секретные фишки Cursor:**

### **1. AI Chat (у вас уже есть!)**
- `Ctrl+L` - Открыть AI чат
- `Ctrl+K` - Inline AI редактирование
- `Ctrl+Shift+L` - AI для выделенного кода

### **2. Composer (новое!)**
- `Ctrl+I` - Открыть Composer
- Создает файлы и код на основе описания

### **3. Codebase Indexing**
- Cursor автоматически индексирует ваш проект
- AI понимает контекст всего проекта

### **4. Terminal Integration**
- Встроенный терминал с AI поддержкой
- Может предлагать команды

---

## 📊 **Для работы с админкой:**

### **1. Live Server**
```json
"ritwickdey.liveserver"  // Запуск локального сервера
```

### **2. Browser Preview**
```json
"auchenberg.vscode-browser-preview"  // Превью в Cursor
```

### **3. REST Client**
```json
"humao.rest-client"  // Тестирование API endpoints
```

---

## 🎨 **Для фронтенда:**

### **1. Color Highlight**
```json
"naumovs.color-highlight"  // Подсветка цветов в CSS
```

### **2. Auto Close Tag**
```json
"formulahendry.auto-close-tag"  // Автозакрытие тегов
```

---

## 🔐 **Безопасность:**

### **1. YAML Support**
```json
"redhat.vscode-yaml"  // Для render.yaml
```

### **2. Environment Variables**
- Используйте `.env` файлы (не коммитьте!)
- Cursor автоматически их распознает

---

## 📈 **Мониторинг и аналитика:**

### **1. TODO Tree**
- Находит все `TODO`, `FIXME`, `BUG` комментарии
- Полезно для отслеживания задач

### **2. Error Lens**
- Показывает ошибки прямо в коде
- Экономит время на отладку

---

## 🚀 **Быстрый старт:**

1. **Установите критичные расширения:**
   ```bash
   # Python, PHP, ESLint, Prettier
   ```

2. **Настройте форматирование:**
   - Создайте `.prettierrc` для единого стиля
   - Настройте автоформатирование при сохранении

3. **Подключите Supabase:**
   - Используйте Database Client расширение
   - Настройте подключение к PostgreSQL

4. **Настройте Git:**
   - GitLens покажет историю изменений
   - Git Graph для визуализации веток

---

## 💡 **Полезные команды Cursor:**

- `Ctrl+Shift+P` - Command Palette
- `Ctrl+P` - Быстрый поиск файлов
- `Ctrl+Shift+F` - Поиск по всему проекту
- `Ctrl+K Ctrl+S` - Keyboard Shortcuts
- `Ctrl+,` - Settings

---

## 🎯 **Следующие шаги:**

1. ✅ Установите рекомендованные расширения
2. ✅ Настройте форматирование кода
3. ✅ Подключите Supabase через Database Client
4. ✅ Используйте AI Chat для быстрой разработки
5. ✅ Настройте GitLens для отслеживания изменений

---

**Удачи с подготовкой к майннету! 🚀**



