# 🌍 Разница между обычной страницей и `?lang=zh`

## 📋 Основные отличия:

### **1. Обычная страница: `solanatamagotchi.com/tamagotchi-game.html`**

**Как определяется язык:**
1. ✅ **localStorage** - сохраненный язык пользователя (`gameLanguage`)
2. ✅ **Telegram WebApp** - язык из Telegram (`language_code`)
3. ✅ **Браузер** - язык браузера (`navigator.language`)
4. ⚠️ **По умолчанию** - английский (`en`)

**Проблема:** Если пользователь из Китая открывает страницу впервые:
- ❌ Telegram недоступен → не определяется
- ❌ Браузер может быть на английском → не определяется
- ❌ localStorage пустой → не определяется
- ❌ **Результат:** Страница на английском языке

---

### **2. Страница с параметром: `solanatamagotchi.com/tamagotchi-game.html?lang=zh`**

**Как определяется язык:**
1. ✅ **URL параметр** - `?lang=zh` (приоритет #1)
2. ✅ **localStorage** - сохраненный язык
3. ✅ **Telegram WebApp** - язык из Telegram
4. ✅ **Браузер** - язык браузера
5. ⚠️ **По умолчанию** - английский

**Преимущества:**
- ✅ **Гарантированный язык** - всегда китайский, независимо от настроек
- ✅ **Прямая ссылка** - можно использовать в постах/рекламе
- ✅ **SEO** - поисковики видят параметр языка
- ✅ **Аналитика** - можно отслеживать переходы по языкам

---

## 🔧 Текущая реализация:

### **Проблема:**
В `js/i18n.js` функция `initI18n()` **НЕ проверяет URL параметр** `?lang=zh`!

```javascript
function initI18n() {
    let lang = 'en';
    
    // 1. Check localStorage
    const savedLang = localStorage.getItem('gameLanguage');
    if (savedLang && SUPPORTED_LANGUAGES[savedLang]) {
        lang = savedLang;
    }
    // 2. Check Telegram WebApp
    else if (window.Telegram?.WebApp?.initDataUnsafe?.user?.language_code) {
        // ...
    }
    // 3. Check browser language
    else {
        // ...
    }
    
    // ❌ НЕТ ПРОВЕРКИ URL ПАРАМЕТРА!
}
```

### **Решение:**
Нужно добавить проверку URL параметра **ПЕРВОЙ** (высший приоритет):

```javascript
function initI18n() {
    let lang = 'en';
    
    // 1. ✅ ПРИОРИТЕТ #1: URL параметр (самый высокий приоритет)
    const urlParams = new URLSearchParams(window.location.search);
    const urlLang = urlParams.get('lang');
    if (urlLang && SUPPORTED_LANGUAGES[urlLang]) {
        lang = urlLang;
        // Сохранить в localStorage для будущих визитов
        localStorage.setItem('gameLanguage', urlLang);
    }
    // 2. Check localStorage
    else if (savedLang && SUPPORTED_LANGUAGES[savedLang]) {
        lang = savedLang;
    }
    // 3. Check Telegram WebApp
    // ...
}
```

---

## 📊 Сравнительная таблица:

| Критерий | Обычная страница | `?lang=zh` |
|----------|------------------|------------|
| **Определение языка** | Автоматическое | Принудительное |
| **Для китайских пользователей** | ❌ Может быть английский | ✅ Всегда китайский |
| **Прямая ссылка** | ❌ Нет | ✅ Да |
| **SEO** | ⚠️ Нейтрально | ✅ Лучше |
| **Аналитика** | ⚠️ Сложнее | ✅ Легче отслеживать |

---

## 🎯 Рекомендации:

### **Для китайских пользователей:**
✅ **ВСЕГДА использовать:** `solanatamagotchi.com/tamagotchi-game.html?lang=zh`

**Где использовать:**
- 📱 Посты в Weibo
- 📱 Посты в Douyin
- 📱 QR-коды
- 📱 Реклама
- 📱 Партнерские ссылки

### **Для остальных пользователей:**
✅ Можно использовать обычную ссылку (автоопределение работает)

---

## 🔧 Что нужно исправить:

1. ✅ Добавить проверку URL параметра в `initI18n()`
2. ✅ Убедиться, что параметр сохраняется в localStorage
3. ✅ Протестировать работу с `?lang=zh`

---

## 📝 Примеры ссылок:

### **Китайский язык:**
```
https://solanatamagotchi.com/tamagotchi-game.html?lang=zh
https://solanatamagotchi.com/mint.html?lang=zh
```

### **Русский язык:**
```
https://solanatamagotchi.com/tamagotchi-game.html?lang=ru
```

### **Английский (по умолчанию):**
```
https://solanatamagotchi.com/tamagotchi-game.html
https://solanatamagotchi.com/tamagotchi-game.html?lang=en
```

