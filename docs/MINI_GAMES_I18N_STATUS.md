# 🎮 Статус локализации мини-игр

**Дата:** Декабрь 2025
**Статус:** 🟡 В процессе

---

## ✅ ЧТО СДЕЛАНО:

### 1. **Добавлены переводы в `js/i18n.js`:**

- ✅ `game_over` - Игра окончена
- ✅ `play_again` - Играть снова
- ✅ `select_bet` - Выберите ставку
- ✅ `choose_bet` - Выберите вашу ставку
- ✅ `enter_amount` - Введите сумму
- ✅ `enter_amount_placeholder` - Введите сумму...
- ✅ `max_bet_hint` - Макс: 50% от баланса
- ✅ `score` - Счет
- ✅ `level` - Уровень
- ✅ `wave` - Волна
- ✅ `lives` - Жизни
- ✅ `enemies` - Враги
- ✅ `start_game` - Начать игру
- ✅ `win_up_to` - Выиграй до
- ✅ `spin_and_win` - Крути и выигрывай
- ✅ `progressive_jackpot` - Прогрессивный джекпот
- ✅ `jackpot_grows` - 5% от каждой ставки увеличивает джекпот
- ✅ `connect_wallet` - Подключить кошелек
- ✅ `connected_wallet` - Подключенный кошелек
- ✅ `disconnect` - Отключить
- ✅ `free_spins` - Бесплатные спины

### 2. **Начата локализация `slots.html`:**

- ✅ Подключен `js/i18n.js`
- ✅ Добавлена инициализация i18n в функцию `initialize()`
- ✅ Заменены основные тексты на `data-i18n` атрибуты:
  - Заголовок игры
  - Кнопка подключения кошелька
  - Баланс
  - Бесплатные спины
  - Всего выиграно
  - Джекпот пул
  - Выбор ставки
  - Custom bet

---

## 🚧 ЧТО НУЖНО СДЕЛАТЬ:

### 1. **Завершить локализацию `slots.html`:**

- [ ] Заменить тексты в JavaScript (динамические сообщения)
- [ ] Заменить тексты в результатах ("You won!", "Try again!")
- [ ] Заменить тексты в истории спинов
- [ ] Заменить тексты в таблице выплат

### 2. **Локализовать `wheel.html`:**

- [ ] Подключить `js/i18n.js`
- [ ] Добавить инициализацию i18n
- [ ] Заменить все тексты на `data-i18n` атрибуты
- [ ] Заменить тексты в JavaScript

### 3. **Локализовать `tama-shooter.html`:**

- [ ] Подключить `js/i18n.js`
- [ ] Добавить инициализацию i18n
- [ ] Заменить все тексты на `data-i18n` атрибуты
- [ ] Заменить тексты в JavaScript (Game Over, Score, Level, Wave, Lives)

### 4. **Локализовать `super-tama-bros.html`:**

- [ ] Подключить `js/i18n.js`
- [ ] Добавить инициализацию i18n
- [ ] Заменить все тексты

### 5. **Локализовать `tama-color-match.html`:**

- [ ] Подключить `js/i18n.js`
- [ ] Добавить инициализацию i18n
- [ ] Заменить все тексты

---

## 📝 ИНСТРУКЦИЯ ДЛЯ БЫСТРОЙ ЛОКАЛИЗАЦИИ:

### Шаг 1: Подключить i18n.js

В `<head>` каждого файла мини-игры добавить:

```html
<script src="js/i18n.js"></script>
```

### Шаг 2: Добавить инициализацию

В функцию `initialize()` или при загрузке страницы добавить:

```javascript
if (typeof initI18n === 'function') {
  initI18n();
  updatePageTranslations();
}
```

### Шаг 3: Заменить тексты в HTML

Заменить хардкодные тексты на `data-i18n` атрибуты:

```html
<!-- Было: -->
<div>Balance</div>

<!-- Стало: -->
<div data-i18n="your_balance">Balance</div>
```

### Шаг 4: Заменить тексты в JavaScript

Использовать функцию `t()` для динамических текстов:

```javascript
// Было:
showResult('You won!', true);

// Стало:
showResult(t('you_won'), true);
```

---

## 🔧 ДОБАВИТЬ НЕДОСТАЮЩИЕ ПЕРЕВОДЫ:

Если найдутся тексты без переводов, добавить в `js/i18n.js`:

```javascript
'new_key': {
    en: 'English text',
    ru: 'Русский текст',
    zh: '中文文本',
    // ... остальные языки
}
```

---

## ⚠️ ВАЖНО:

1. **Проверить язык из URL:**

   - Мини-игры должны определять язык из параметра `?lang=zh`
   - Или из `localStorage.getItem('gameLanguage')`

2. **Обновлять переводы динамически:**

   - Если язык меняется, нужно вызвать `updatePageTranslations()`

3. **Тестировать:**
   - Открыть каждую игру с `?lang=zh`
   - Проверить все тексты переведены

---

**Создано:** Декабрь 2025
**Приоритет:** 🔴 ВЫСОКИЙ (для китайского рынка)
