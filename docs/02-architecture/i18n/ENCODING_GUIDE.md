# 🔧 Как избежать крякозябр (кодировка UTF-8)

## ❌ ПРОБЛЕМА:
Когда используем `Out-File` в PowerShell, появляются крякозябры типа "рџЏЁ", "вњ…"

## ✅ РЕШЕНИЕ:

### 1. ВСЕГДА используй правильный метод записи:

```powershell
# ❌ ПЛОХО (создаёт BOM и крякозябры):
$content | Out-File -FilePath "file.html" -Encoding UTF8

# ✅ ХОРОШО (без BOM):
$utf8NoBom = New-Object System.Text.UTF8Encoding $false
[System.IO.File]::WriteAllText("$PWD\file.html", $content, $utf8NoBom)
```

### 2. Используй helper-функцию:

```powershell
# Загрузи helper
. .\utf8-helper.ps1

# Сохраняй файлы правильно
Save-UTF8NoBOM -FilePath "file.html" -Content $content
```

### 3. Конвертируй все файлы разом:

```powershell
. .\utf8-helper.ps1
Convert-AllToUTF8NoBOM -Directory "."
```

## 📝 Что такое BOM?

**BOM (Byte Order Mark)** - это специальные байты (EF BB BF) в начале файла.
- Некоторые редакторы добавляют BOM
- Браузеры могут неправильно интерпретировать эмодзи с BOM
- UTF-8 **без BOM** - стандарт для веб-разработки

## 🔍 Проверка файлов:

```powershell
# Проверить первые байты
$bytes = [System.IO.File]::ReadAllBytes("file.html")
Write-Host "First bytes: $($bytes[0]) $($bytes[1]) $($bytes[2])"

# Если 239 187 191 = есть BOM (плохо)
# Если другие значения = нет BOM (хорошо)
```

## 🎯 ВАЖНО ДЛЯ БУДУЩЕГО:

1. **Всегда** используй `[System.IO.File]::WriteAllText()` с UTF8Encoding($false)
2. **Никогда** не используй просто `Out-File` или `>` для HTML/JS/CSS
3. После любых изменений файлов - проверяй в браузере с Ctrl+F5

---

**Теперь крякозябр не будет! ✅**