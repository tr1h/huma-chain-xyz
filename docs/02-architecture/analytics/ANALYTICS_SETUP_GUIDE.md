# 📊 Руководство по настройке аналитики для китайского рынка

**Дата:** Декабрь 2025  
**Статус:** ✅ Google Analytics подключен и настроен

---

## ✅ ЧТО УЖЕ НАСТРОЕНО:

### 1. **Google Analytics**
- ✅ Подключен: `G-L7LVQM50LV`
- ✅ Отслеживание просмотров страниц
- ✅ Отслеживание языка из URL параметра (`?lang=zh`)
- ✅ Отслеживание подключения кошелька
- ✅ Отслеживание создания аккаунта

### 2. **Кастомный трекинг**
- ✅ API: `api/track-visit.php`
- ✅ Сохранение в Supabase таблицу `site_visits`
- ✅ Отслеживание: URL, referrer, user agent, device, browser, OS

---

## 📊 КАК ПРОСМАТРИВАТЬ СТАТИСТИКУ:

### **Google Analytics:**

**1. Войдите в Google Analytics:**
- URL: https://analytics.google.com/
- Аккаунт: [ваш аккаунт]
- ID: `G-L7LVQM50LV`

**2. Основные отчеты:**

**Реальное время (Real-time):**
- Переходы: `Reports → Real-time → Overview`
- События: `Reports → Real-time → Events`
- Языки: `Reports → Real-time → Locations` (можно фильтровать по языку)

**Аудитория (Audience):**
- Активные пользователи: `Reports → Audience → Overview`
- География: `Reports → Audience → Geo → Location`
- Устройства: `Reports → Audience → Technology → Browser & OS`

**Поведение (Behavior):**
- Просмотры страниц: `Reports → Behavior → Site Content → All Pages`
- События: `Reports → Behavior → Events → Overview`

**3. Отслеживание китайских пользователей:**

**Фильтр по языку:**
1. Перейдите в `Reports → Real-time → Events`
2. Найдите событие `language_detected`
3. Фильтр: `language = zh`

**Фильтр по странице:**
1. Перейдите в `Reports → Behavior → Site Content → All Pages`
2. Фильтр: `page = /tamagotchi-game.html?lang=zh`

**4. Отслеживание событий:**

**Подключение кошелька:**
- Событие: `wallet_connected`
- Параметры: `wallet_type`, `is_new_user`, `language`

**Создание аккаунта:**
- Событие: `account_created`
- Параметры: `auth_method`, `wallet_type`, `language`

**Язык:**
- Событие: `language_detected`
- Параметры: `language`, `source`

---

## 📈 КАСТОМНЫЕ ОТЧЕТЫ (рекомендуется создать):

### **Отчет 1: Китайские пользователи**

**Настройка:**
1. `Customization → Custom Reports → Create`
2. Название: "Chinese Users"
3. Метрики: Sessions, Users, Pageviews
4. Измерения: Language, Page, Device Category
5. Фильтр: `Language = zh` ИЛИ `Page contains ?lang=zh`

### **Отчет 2: Wallet подключения**

**Настройка:**
1. `Customization → Custom Reports → Create`
2. Название: "Wallet Connections"
3. Метрики: Event Count, Users
4. Измерения: Event Label, Language
5. Фильтр: `Event = wallet_connected`

### **Отчет 3: Конверсии (регистрации)**

**Настройка:**
1. `Customization → Custom Reports → Create`
2. Название: "Account Creations"
3. Метрики: Event Count, Users
4. Измерения: Event Label, Language, Wallet Type
5. Фильтр: `Event = account_created`

---

## 🔍 КАК ОТСЛЕЖИВАТЬ КИТАЙСКИХ ПОЛЬЗОВАТЕЛЕЙ:

### **Метод 1: По языку (URL параметр)**

**В Google Analytics:**
1. `Reports → Behavior → Events → language_detected`
2. Фильтр: `language = zh`
3. Смотрите количество событий и пользователей

### **Метод 2: По странице**

**В Google Analytics:**
1. `Reports → Behavior → Site Content → All Pages`
2. Фильтр: `page = /tamagotchi-game.html?lang=zh`
3. Смотрите просмотры и уникальные посетители

### **Метод 3: По географии**

**В Google Analytics:**
1. `Reports → Audience → Geo → Location`
2. Фильтр: `Country = China`
3. Смотрите сессии и пользователей

### **Метод 4: В Supabase (кастомный трекинг)**

**Запрос к таблице `site_visits`:**
```sql
-- Все посещения с ?lang=zh
SELECT * FROM site_visits 
WHERE page_url LIKE '%lang=zh%'
ORDER BY visit_time DESC;

-- Уникальные посетители за день
SELECT COUNT(DISTINCT session_id) as unique_visitors
FROM site_visits
WHERE page_url LIKE '%lang=zh%'
AND visit_date = CURRENT_DATE;

-- Статистика по дням
SELECT 
    visit_date,
    COUNT(*) as total_visits,
    COUNT(DISTINCT session_id) as unique_visitors
FROM site_visits
WHERE page_url LIKE '%lang=zh%'
GROUP BY visit_date
ORDER BY visit_date DESC;
```

---

## 📊 КЛЮЧЕВЫЕ МЕТРИКИ ДЛЯ ОТСЛЕЖИВАНИЯ:

### **Ежедневно проверяйте:**

1. **Просмотры страницы `/tamagotchi-game.html?lang=zh`**
   - Цель: 100+ в день (первая неделя)
   - Где: `Reports → Behavior → Site Content → All Pages`

2. **События `wallet_connected`**
   - Цель: 10+ в день (первая неделя)
   - Где: `Reports → Behavior → Events → wallet_connected`

3. **События `account_created`**
   - Цель: 5+ в день (первая неделя)
   - Где: `Reports → Behavior → Events → account_created`

4. **События `language_detected` с `language=zh`**
   - Цель: 50+ в день (первая неделя)
   - Где: `Reports → Behavior → Events → language_detected`

### **Еженедельно проверяйте:**

1. **География пользователей**
   - Китай должен быть в топ-5 стран
   - Где: `Reports → Audience → Geo → Location`

2. **Источники трафика**
   - Откуда приходят китайские пользователи
   - Где: `Reports → Acquisition → All Traffic → Source/Medium`

3. **Конверсия (регистрации / просмотры)**
   - Цель: 5-10% конверсия
   - Формула: `account_created / pageviews * 100`

---

## 🎯 НАСТРОЙКА ЦЕЛЕЙ (GOALS):

### **Цель 1: Подключение кошелька**

**Настройка:**
1. `Admin → Goals → New Goal`
2. Тип: `Event`
3. Условие: `Event = wallet_connected`
4. Название: "Wallet Connection"

### **Цель 2: Создание аккаунта**

**Настройка:**
1. `Admin → Goals → New Goal`
2. Тип: `Event`
3. Условие: `Event = account_created`
4. Название: "Account Creation"

### **Цель 3: Китайский пользователь**

**Настройка:**
1. `Admin → Goals → New Goal`
2. Тип: `Event`
3. Условие: `Event = language_detected` AND `language = zh`
4. Название: "Chinese User"

---

## 📱 МОБИЛЬНОЕ ПРИЛОЖЕНИЕ GOOGLE ANALYTICS:

**Скачайте:**
- iOS: App Store → "Google Analytics"
- Android: Google Play → "Google Analytics"

**Преимущества:**
- ✅ Просмотр статистики в реальном времени
- ✅ Уведомления о важных событиях
- ✅ Быстрый доступ к отчетам

---

## 🔔 НАСТРОЙКА УВЕДОМЛЕНИЙ:

### **Email отчеты:**

**Настройка:**
1. `Admin → View → Intelligence Events`
2. Создайте уведомление:
   - Условие: `wallet_connected > 10 за день`
   - Email: [ваш email]
   - Частота: Ежедневно

### **Уведомления в приложении:**

**Настройка:**
1. Откройте мобильное приложение
2. `Settings → Intelligence Alerts`
3. Включите уведомления для:
   - Новые события
   - Рост трафика
   - Важные изменения

---

## 📊 АЛЬТЕРНАТИВНЫЕ ИНСТРУМЕНТЫ:

### **1. Supabase Dashboard (кастомный трекинг)**

**Доступ:**
- URL: https://supabase.com/dashboard
- Проект: [ваш проект]
- Таблица: `site_visits`

**Что можно смотреть:**
- Все посещения с деталями
- Фильтры по URL, дате, устройству
- Экспорт данных

### **2. Google Search Console (SEO)**

**Доступ:**
- URL: https://search.google.com/search-console
- Добавьте сайт: `solanatamagotchi.com`

**Что можно смотреть:**
- Поисковые запросы
- Позиции в поиске
- Клики из поиска

---

## 🎯 ЧЕКЛИСТ НАСТРОЙКИ:

- [ ] Проверить доступ к Google Analytics
- [ ] Создать кастомные отчеты для китайских пользователей
- [ ] Настроить цели (Goals)
- [ ] Настроить уведомления
- [ ] Скачать мобильное приложение
- [ ] Проверить работу событий (wallet_connected, account_created)
- [ ] Настроить фильтры по языку

---

## 📞 ПОМОЩЬ:

**Если статистика не отображается:**

1. **Проверьте код:**
   - Откройте страницу
   - F12 → Console
   - Проверьте, нет ли ошибок с `gtag`

2. **Проверьте фильтры:**
   - В Google Analytics могут быть активные фильтры
   - `Admin → View → Filters`

3. **Проверьте права доступа:**
   - Убедитесь, что у вас есть доступ к аккаунту

---

**Создано:** Декабрь 2025  
**Следующий шаг:** Проверить статистику в Google Analytics (5 минут)

