# 📁 Организация NFT файлов - Best Practices

## 🎯 Стандарты индустрии:

### **Вариант 1: По Tier и Rarity (РЕКОМЕНДУЕТСЯ) ✅**

```
nft-assets/
├── generated/                    ← Сгенерированные изображения (локально)
│   ├── bronze/
│   │   ├── common.png
│   │   ├── uncommon.png
│   │   ├── rare.png
│   │   ├── epic.png
│   │   └── legendary.png
│   ├── silver/
│   │   ├── common.png
│   │   ├── uncommon.png
│   │   ├── rare.png
│   │   ├── epic.png
│   │   └── legendary.png
│   ├── gold/
│   │   └── ...
│   └── platinum/
│       └── ...
│
├── ipfs/                         ← После загрузки на IPFS (backup URLs)
│   └── ipfs-urls.json
│
└── metadata/                     ← Метаданные (если нужно)
    └── ...
```

**Преимущества:**
- ✅ Легко найти нужный файл
- ✅ Логичная структура
- ✅ Соответствует структуре кода
- ✅ Стандарт для NFT проектов

---

### **Вариант 2: Плоская структура (проще, но менее организовано)**

```
nft-assets/
├── generated/
│   ├── bronze-common.png
│   ├── bronze-uncommon.png
│   ├── bronze-rare.png
│   ├── bronze-epic.png
│   ├── bronze-legendary.png
│   ├── silver-common.png
│   ├── silver-uncommon.png
│   └── ... (20 файлов всего)
```

**Преимущества:**
- ✅ Все в одном месте
- ✅ Легко загружать на IPFS
- ✅ Проще для скриптов

**Недостатки:**
- ❌ Менее организовано
- ❌ Сложнее найти при большом количестве

---

## 🏆 РЕКОМЕНДАЦИЯ: Вариант 1 (по папкам)

### **Структура:**

```
nft-assets/
├── generated/                    ← СЮДА КЛАДИ СГЕНЕРИРОВАННЫЕ
│   ├── bronze/
│   │   ├── common.png
│   │   ├── uncommon.png
│   │   ├── rare.png
│   │   ├── epic.png
│   │   └── legendary.png
│   ├── silver/
│   │   ├── common.png
│   │   ├── uncommon.png
│   │   ├── rare.png
│   │   ├── epic.png
│   │   └── legendary.png
│   ├── gold/
│   │   └── ... (5 файлов)
│   └── platinum/
│       └── ... (5 файлов)
│
├── ipfs/                         ← IPFS URLs после загрузки
│   └── ipfs-urls.json
│
└── metadata/                     ← Метаданные (опционально)
    └── ...
```

---

## 📝 Названия файлов:

### **Стандарт 1: `{tier}-{rarity}.png` (РЕКОМЕНДУЕТСЯ)**

```
bronze-common.png
bronze-uncommon.png
bronze-rare.png
bronze-epic.png
bronze-legendary.png
silver-common.png
...
```

**Почему:**
- ✅ Понятно и читаемо
- ✅ Легко сортировать
- ✅ Стандарт индустрии
- ✅ Работает с кодом

### **Стандарт 2: `{tier}_{rarity}.png`**

```
bronze_common.png
bronze_uncommon.png
...
```

**Почему:**
- ✅ Подчеркивание вместо дефиса
- ✅ Некоторые проекты так делают

### **Стандарт 3: `{tier}-{rarity}-{number}.png` (если несколько вариантов)**

```
bronze-common-1.png
bronze-common-2.png
...
```

**Когда использовать:**
- Если у тебя несколько вариантов одного tier/rarity
- Для вариаций дизайна

---

## 🎯 ИТОГОВАЯ РЕКОМЕНДАЦИЯ:

### **Структура папок:**
```
nft-assets/generated/
├── bronze/
│   ├── common.png
│   ├── uncommon.png
│   ├── rare.png
│   ├── epic.png
│   └── legendary.png
├── silver/
│   └── ...
├── gold/
│   └── ...
└── platinum/
    └── ...
```

### **Названия файлов:**
```
{tier}-{rarity}.png

Примеры:
- bronze-common.png
- silver-rare.png
- gold-legendary.png
- platinum-epic.png
```

---

## 📤 После загрузки на IPFS:

### **Структура IPFS URLs:**

```json
{
  "bronze": {
    "common": "https://ipfs.io/ipfs/Qm.../bronze-common.png",
    "uncommon": "https://ipfs.io/ipfs/Qm.../bronze-uncommon.png",
    ...
  },
  "silver": {
    ...
  },
  "gold": {
    ...
  },
  "platinum": {
    ...
  }
}
```

**Сохрани в:**
```
nft-assets/ipfs/ipfs-urls.json
```

---

## 🔧 Обновление скрипта загрузки:

Скрипт `upload-to-ipfs.js` должен поддерживать структуру:

```javascript
// Загрузка из папок
const tiers = ['bronze', 'silver', 'gold', 'platinum'];
const rarities = ['common', 'uncommon', 'rare', 'epic', 'legendary'];

for (const tier of tiers) {
    for (const rarity of rarities) {
        const filePath = `generated/${tier}/${rarity}.png`;
        // Загрузить на IPFS
    }
}
```

---

## 📋 Чеклист:

- [ ] Создал папку `nft-assets/generated/`
- [ ] Создал подпапки: `bronze/`, `silver/`, `gold/`, `platinum/`
- [ ] Сохранил файлы с названиями: `{tier}-{rarity}.png`
- [ ] Проверил что все 20 файлов на месте
- [ ] Загрузил на IPFS
- [ ] Сохранил IPFS URLs в `nft-assets/ipfs/ipfs-urls.json`

---

## 💡 Примеры из популярных NFT проектов:

### **Bored Ape Yacht Club:**
```
images/
├── 1.png
├── 2.png
└── ...
```

### **CryptoPunks:**
```
punks/
├── punk-1.png
├── punk-2.png
└── ...
```

### **Azuki:**
```
azuki/
├── 1.png
├── 2.png
└── ...
```

**Но для твоего проекта лучше:**
```
generated/
├── bronze/
│   └── common.png
└── ...
```

**Почему:**
- У тебя структурированные tiers и rarities
- Легче управлять
- Соответствует логике игры

---

## 🚀 Финальная структура:

```
nft-assets/
├── generated/              ← СГЕНЕРИРОВАННЫЕ ИЗОБРАЖЕНИЯ (локально)
│   ├── bronze/
│   │   ├── common.png
│   │   ├── uncommon.png
│   │   ├── rare.png
│   │   ├── epic.png
│   │   └── legendary.png
│   ├── silver/
│   │   └── ... (5 файлов)
│   ├── gold/
│   │   └── ... (5 файлов)
│   └── platinum/
│       └── ... (5 файлов)
│
├── ipfs/                   ← IPFS URLs (после загрузки)
│   └── ipfs-urls.json
│
├── sora-tamagotchi-device-prompts.csv  ← Промпты
├── upload-to-ipfs.js       ← Скрипт загрузки
└── STEP_BY_STEP_GUIDE.md   ← Инструкция
```

---

## ✅ ИТОГО:

**Структура:** `nft-assets/generated/{tier}/{rarity}.png`  
**Названия:** `{tier}-{rarity}.png` (например: `bronze-common.png`)  
**Всего:** 20 файлов (4 tiers × 5 rarities)

**Это стандарт индустрии и лучшая практика!** 🎯






