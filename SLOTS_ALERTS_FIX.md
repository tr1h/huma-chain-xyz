# 🔧 ИСПРАВЛЕНИЕ АЛЕРТОВ - CHECKLIST

## ❌ ПРОБЛЕМА:

Выигрыш x10 был, но алерт не пришел в канал/группу.

## 🔍 ВОЗМОЖНЫЕ ПРИЧИНЫ:

### 1. Бот не добавлен в канал/группу
```
Решение:
1. Открой канал @GotchiGame
2. Добавь бота как администратора
3. Дай права: "Post messages"

То же для группы @gotchigamechat
```

### 2. Неправильные ID канала/группы в bot.py
```python
# Проверь в bot.py:
CHANNEL_USERNAME = '@GotchiGame'  # Должно быть правильное
GROUP_ID = -1002938566588  # Должен быть правильный ID
```

### 3. Бот не перезапущен после изменений
```bash
# Если бот на Render/Railway - подожди 2-3 минуты
# Если локально - перезапусти вручную
```

### 4. Алерты работают только в боте, не в WebApp
```
Проблема: API не отправляет webhook в бота

Решение: Добавить BOT_WEBHOOK_URL в переменные окружения
```

---

## ✅ КАК ПРОВЕРИТЬ:

### Шаг 1: Проверь права бота
```
1. Открой @GotchiGame
2. Настройки → Администраторы
3. Найди своего бота
4. Убедись что есть галочка "Post messages"
```

### Шаг 2: Проверь ID группы
```python
# В bot.py должно быть:
GROUP_ID = int(os.getenv('GROUP_ID', '-1002938566588'))

# Проверь что это правильный ID группы @gotchigamechat
```

### Шаг 3: Тест в боте
```
1. Открой бота в Telegram
2. /slots
3. Сделай ставку
4. Если выпадет x10+ → должен быть алерт
```

### Шаг 4: Тест в WebApp
```
1. Открой https://solanatamagotchi.com/slots.html
2. Сделай ставку
3. Если выпадет x10+ → алерт может НЕ прийти (нужен webhook)
```

---

## 🔧 БЫСТРОЕ ИСПРАВЛЕНИЕ:

### Если алерты не работают ВООБЩЕ:

```python
# В bot.py добавь тестовую функцию:
def test_alert():
    try:
        bot.send_message(CHANNEL_USERNAME, "🧪 Test alert from bot")
        bot.send_message(GROUP_ID, "🧪 Test alert from bot")
        print("✅ Test alerts sent!")
    except Exception as e:
        print(f"❌ Error: {e}")

# Вызови в консоли:
test_alert()
```

Если ошибка:
- `Forbidden: bot is not a member` → добавь бота в канал/группу
- `Chat not found` → неправильный ID
- `Bot was blocked` → разблокируй бота

---

## 🎯 WEBHOOK ДЛЯ WEBAPP:

Чтобы алерты работали из slots.html, нужно:

### 1. Добавить Flask endpoint в bot.py:
```python
@app.route('/jackpot-alert', methods=['POST'])
def jackpot_alert_webhook():
    data = request.json
    if data.get('type') == 'jackpot_win':
        send_jackpot_alert(
            data['telegram_id'],
            data['username'],
            data['first_name'],
            data['jackpot_amount'],
            data['total_win'],
            data['bet_amount'],
            data['pool_before']
        )
    return {'success': True}

@app.route('/big-win-alert', methods=['POST'])
def big_win_alert_webhook():
    data = request.json
    if data.get('type') == 'big_win':
        send_big_win_alert(
            data['telegram_id'],
            data['username'],
            data['first_name'],
            data['win_amount'],
            data['multiplier'],
            data['symbols']
        )
    return {'success': True}
```

### 2. Установить BOT_WEBHOOK_URL:
```bash
# В переменных окружения (Render/Railway):
BOT_WEBHOOK_URL=https://your-bot-url.com
```

### 3. API будет слать webhook:
```php
// В api/tama_supabase.php уже есть:
$botWebhook = getenv('BOT_WEBHOOK_URL') ?: 'https://your-bot-webhook.com/jackpot-alert';
```

---

## 📊 ЛОГИ ДЛЯ ОТЛАДКИ:

### В bot.py уже есть логи:
```python
print(f"✅ Jackpot alert sent to channel: {CHANNEL_USERNAME}")
print(f"✅ Jackpot alert sent to group: {GROUP_ID}")
print(f"⚠️ Failed to send to channel: {e}")
```

### Проверь логи бота:
```bash
# Если бот на Render:
# Render Dashboard → Logs

# Если локально:
# Смотри в терминал где запущен бот
```

---

## 🎯 ИТОГОВЫЙ CHECKLIST:

- [ ] Бот добавлен в @GotchiGame как админ
- [ ] Бот добавлен в @gotchigamechat как админ
- [ ] Права "Post messages" включены
- [ ] CHANNEL_USERNAME и GROUP_ID правильные
- [ ] Бот перезапущен после изменений
- [ ] Тест алерт отправлен успешно
- [ ] BOT_WEBHOOK_URL настроен (для WebApp)
- [ ] Логи проверены

---

## 🚀 ПОСЛЕ ИСПРАВЛЕНИЯ:

1. Сделай тестовый спин с x10+
2. Проверь канал - должен быть алерт
3. Проверь группу - должен быть алерт (только для джекпота)
4. Если не работает - проверь логи бота

**Удачи!** 🎰

