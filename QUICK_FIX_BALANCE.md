# 🔧 БЫСТРОЕ ИСПРАВЛЕНИЕ БАЛАНСА

## Проблема:
Бот показывает баланс, но slots.html показывает 0.

## Причина:
API не находит пользователя в таблице `leaderboard`.

---

## ✅ РЕШЕНИЕ (2 способа):

### Способ 1: Через Supabase (БЫСТРО)

1. Откройте Supabase: https://supabase.com/dashboard
2. Выберите проект
3. Перейдите в **SQL Editor**
4. Выполните этот SQL:

```sql
-- Проверить, есть ли пользователь
SELECT telegram_id, tama FROM leaderboard WHERE telegram_id = '2139640084';

-- Если НЕТ записи, создать:
INSERT INTO leaderboard (telegram_id, tama)
VALUES ('2139640084', 10000)
ON CONFLICT (telegram_id) DO UPDATE SET tama = 10000;

-- Проверить результат:
SELECT telegram_id, tama FROM leaderboard WHERE telegram_id = '2139640084';
```

5. Обновите slots.html (F5)

---

### Способ 2: Через бота (АВТОМАТИЧЕСКИ)

Просто напишите в боте:

```
/start
```

Бот автоматически создаст запись в `leaderboard`.

Затем обновите slots.html (F5).

---

## 🔍 ДИАГНОСТИКА

### Проверить API напрямую:

Откройте в браузере:
```
https://api.solanatamagotchi.com/api/tama/balance?telegram_id=2139640084
```

**Должно вернуть:**
```json
{
  "success": true,
  "telegram_id": "2139640084",
  "database_tama": 10000,  // ← не 0!
  "total_tama": 10000
}
```

Если `database_tama: 0` → пользователь не найден в базе.

---

## 🎯 ПОЧЕМУ ТАК ПРОИСХОДИТ?

Бот может хранить баланс в другой таблице или использовать другой формат ID.

**Проверьте в Supabase:**

```sql
-- Найти все записи с похожим ID
SELECT * FROM leaderboard 
WHERE telegram_id LIKE '%2139640084%' 
   OR CAST(telegram_id AS TEXT) = '2139640084';

-- Проверить транзакции
SELECT * FROM transactions 
WHERE telegram_id = '2139640084' 
ORDER BY created_at DESC 
LIMIT 5;
```

Если транзакции есть, но в `leaderboard` нет → нужно создать запись.

---

## ⚡ САМОЕ БЫСТРОЕ РЕШЕНИЕ:

1. Откройте Supabase SQL Editor
2. Вставьте:
```sql
INSERT INTO leaderboard (telegram_id, tama)
VALUES ('2139640084', 10000)
ON CONFLICT (telegram_id) DO UPDATE SET tama = 10000;
```
3. Нажмите **Run**
4. Обновите slots.html

**Готово!** Баланс должен отображаться.

