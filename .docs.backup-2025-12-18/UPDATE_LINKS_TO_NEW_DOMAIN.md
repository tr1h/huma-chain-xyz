# 🔗 Обновление ссылок на новый домен

## ✅ Текущий статус:

- ✅ `solanatamagotchi.com` - работает (HTTP, нужно включить HTTPS)
- ✅ `api.solanatamagotchi.com` - CNAME настроен, нужно проверить

---

## 📋 Что нужно обновить:

### 1. API ссылки:
- **Старый:** `https://huma-chain-xyz.onrender.com/api/`
- **Новый:** `https://api.solanatamagotchi.com/api/`

### 2. Frontend ссылки:
- **Старый:** `https://tr1h.github.io/huma-chain-xyz/`
- **Новый:** `https://solanatamagotchi.com/`

---

## 🔧 Файлы для обновления:

### Приоритет 1 (критично):
- `nft-mint-5tiers.html` - API ссылки для минта NFT
- `tamagotchi-game.html` - API ссылки для игры
- `my-nfts.html` - API ссылки для просмотра NFT
- `js/admin-env.js` - конфигурация API

### Приоритет 2 (желательно):
- `index.html` - ссылки на другие страницы
- `bot/bot.py` - ссылки на игру и минт (GAME_URL, MINT_URL)
- `transactions-admin.html` - API ссылки
- Все остальные HTML файлы

---

## 💡 Рекомендация:

**Использовать автоматическое определение домена:**

```javascript
// Вместо хардкода
const API_BASE = 'https://api.solanatamagotchi.com/api';

// Использовать автоматическое определение
const API_BASE = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1'
    ? 'http://localhost:8002/api'
    : `https://api.${window.location.hostname.replace('solanatamagotchi.com', 'solanatamagotchi.com')}/api`;
```

Или проще:
```javascript
const API_BASE = window.location.hostname.includes('localhost')
    ? 'http://localhost:8002/api'
    : 'https://api.solanatamagotchi.com/api';
```

---

## 🚀 План действий:

1. **Включить HTTPS в GitHub Pages** (5 минут)
2. **Проверить что API работает** (1 минута)
3. **Обновить ссылки в критичных файлах** (15 минут)
4. **Обновить ссылки в bot.py** (5 минут)
5. **Протестировать все функции** (10 минут)

---

**Последнее обновление:** 13 ноября 2024

