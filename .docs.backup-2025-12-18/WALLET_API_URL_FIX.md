# 🔧 Исправление URL для wallet-auth API

## ❌ Проблема

API файл `wallet-auth.php` был указан на GitHub Pages:
```
https://solanatamagotchi.com/api/wallet-auth.php  ❌
```

GitHub Pages **не поддерживает PHP**, поэтому возвращается 404/405 ошибка!

## ✅ Решение

API должен быть на Render.com сервере:
```
https://api.solanatamagotchi.com/api/wallet-auth.php  ✅
```

---

## 📝 Что изменено

### 1. `js/wallet-auth-cn.js`
```javascript
// Было:
const WALLET_AUTH_API = 'https://solanatamagotchi.com/api/wallet-auth.php';

// Стало:
const WALLET_AUTH_API = 'https://api.solanatamagotchi.com/api/wallet-auth.php';
```

### 2. `js/wallet-save-cn.js`
```javascript
// Было:
var WALLET_AUTH_API = 'https://solanatamagotchi.com/api/wallet-auth.php';

// Стало:
var WALLET_AUTH_API = 'https://api.solanatamagotchi.com/api/wallet-auth.php';
```

### 3. `NEW_CHAT_PROMPT.md`
Обновлена документация с правильным URL.

---

## 🚀 Что нужно проверить

### 1. Файл на API сервере
Убедитесь, что `api/wallet-auth.php` загружен на Render.com:
- Файл должен быть в папке `api/` на сервере
- Должен быть доступен по пути: `/api/wallet-auth.php`

### 2. Доступность API
Проверьте в браузере:
```
https://api.solanatamagotchi.com/api/wallet-auth.php
```

**Ожидаемый результат:**
- Должен вернуть JSON (не HTML)
- Может быть ошибка метода (нужен POST), но не 404!

### 3. Тест подключения кошелька
1. Откройте игру
2. Нажмите "Connect Wallet"
3. Подтвердите в Phantom/Solflare
4. Проверьте консоль - не должно быть 404/405 ошибок

---

## 📋 Архитектура

- **Frontend (GitHub Pages):** `https://solanatamagotchi.com/`
  - Только HTML/CSS/JavaScript
  - НЕ поддерживает PHP
  
- **Backend API (Render.com):** `https://api.solanatamagotchi.com/`
  - PHP файлы
  - Обработка запросов
  - Подключение к базе данных

---

## 🔍 Если всё ещё не работает

### Проверка 1: Файл на сервере
Убедитесь, что файл `wallet-auth.php` есть в папке `api/` на Render.com:
- Зайдите в Render.com Dashboard
- Проверьте файлы сервиса
- Убедитесь, что `api/wallet-auth.php` присутствует

### Проверка 2: Конфигурация сервера
Проверьте, что Apache/Nginx настроен правильно:
- Файлы должны быть доступны напрямую
- Нет блокировок для POST запросов

### Проверка 3: CORS
Убедитесь, что CORS настроен правильно в `wallet-auth.php`:
```php
header('Access-Control-Allow-Origin: *');
header('Access-Control-Allow-Methods: POST, OPTIONS');
```

---

**Дата:** 2025-11-29  
**Статус:** ✅ URL исправлен

