# 📋 Гайд по использованию юридических документов

## ✅ ЧТО СДЕЛАНО

### 1. Документы созданы
- ✅ `/terms` - Terms of Service
- ✅ `/privacy` - Privacy Policy
- ✅ `/disclaimer` - Risk Warning

### 2. Footer добавлен на все страницы
- ✅ `index.html`
- ✅ `whitepaper.html`
- ✅ `mint.html`
- ✅ `profile.html`
- ✅ `tamagotchi-game.html`
- ✅ `referral.html`

### 3. Telegram бот обновлен
- ✅ `bot/bot.py` - ссылки в welcome message

### 4. Модальное окно согласия
- ✅ `js/legal-consent.js` - модальное окно для первого запуска
- ✅ Подключено к `tamagotchi-game.html`

### 5. Чекбокс согласия для форм
- ✅ `js/legal-checkbox.js` - компонент для mint/withdrawal
- ✅ Подключен к `mint.html`

---

## 📖 КАК ИСПОЛЬЗОВАТЬ

### Модальное окно согласия (Legal Consent Modal)

**Автоматически показывается при первом запуске игры.**

Что делает:
- Показывается один раз
- Сохраняет согласие в `localStorage`
- Блокирует доступ к игре до согласия
- Ссылки на все юридические документы

**Где используется:**
- `tamagotchi-game.html` - автоматически

**Как отключить (если нужно):**
```javascript
// Удалить из localStorage
localStorage.removeItem('solana_tamagotchi_legal_consent');
// Перезагрузить страницу - модальное окно появится снова
```

---

### Чекбокс согласия для форм (Legal Checkbox)

**Используется перед минтингом NFT и выводом токенов.**

#### Способ 1: Модальное окно подтверждения

```javascript
// Перед минтингом или выводом
async function mintNFT() {
    // Показать модальное окно согласия
    const accepted = await LegalCheckbox.showConsentDialog('mint');
    
    if (!accepted) {
        console.log('User declined terms');
        return; // Прервать операцию
    }
    
    // Продолжить минтинг
    console.log('User accepted terms, proceeding with mint');
    // ... код минтинга
}

// Или для вывода токенов
async function withdrawTokens() {
    const accepted = await LegalCheckbox.showConsentDialog('withdraw');
    
    if (!accepted) {
        return; // Прервать вывод
    }
    
    // Продолжить вывод
    // ... код вывода
}
```

**Типы действий:**
- `'mint'` - "mint NFT"
- `'withdraw'` - "withdraw tokens"
- `'purchase'` - "make a purchase"
- `'action'` - "continue" (общий)

#### Способ 2: Inline чекбокс в форме

```javascript
// Добавить чекбокс прямо в форму
LegalCheckbox.addToForm('mint-form', 'mint-submit-button');

// Это автоматически:
// - Добавит чекбокс перед кнопкой
// - Отключит кнопку до согласия
// - Включит кнопку после согласия
```

---

## 🚀 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### В mint.html (уже подключено)

```javascript
// В функции минтинга NFT
async function mintNFTTier(tier) {
    try {
        // Показать модальное окно согласия
        const accepted = await LegalCheckbox.showConsentDialog('mint');
        
        if (!accepted) {
            showNotification('You must accept the terms to mint NFT', 'warning');
            return;
        }
        
        // Продолжить минтинг
        showNotification('Processing mint...', 'info');
        // ... существующий код минтинга
        
    } catch (error) {
        console.error('Mint error:', error);
    }
}
```

### В withdrawal (пример)

```javascript
// В функции вывода токенов
async function withdrawTAMA(amount) {
    try {
        // Показать модальное окно согласия
        const accepted = await LegalCheckbox.showConsentDialog('withdraw');
        
        if (!accepted) {
            alert('You must accept the terms to withdraw tokens');
            return;
        }
        
        // Продолжить вывод
        console.log('Processing withdrawal of', amount, 'TAMA');
        // ... код вывода
        
    } catch (error) {
        console.error('Withdrawal error:', error);
    }
}
```

---

## 📝 ГДЕ ДОБАВИТЬ ЧЕКБОКС

### Критично (уже сделано или нужно добавить):
1. ✅ **Минтинг NFT** - `mint.html` (подключено)
2. ⚠️ **Вывод токенов** - `withdrawal` функция в боте или на сайте
3. ⚠️ **Покупка TAMA** - если есть

### Опционально:
4. Подключение кошелька (первый раз)
5. Участие в играх с высокими ставками
6. Передача токенов другим пользователям

---

## 🛠️ ИНТЕГРАЦИЯ С СУЩЕСТВУЮЩИМ КОДОМ

### Шаг 1: Подключить скрипт
```html
<script src="js/legal-checkbox.js"></script>
```

### Шаг 2: Использовать перед критической операцией
```javascript
const accepted = await LegalCheckbox.showConsentDialog('action');
if (!accepted) return; // Прервать
```

---

## 🎯 ГОТОВНОСТЬ К MAINNET

### Чеклист:
- [x] Документы созданы
- [x] Footer на всех страницах
- [x] Telegram бот обновлен
- [x] Модальное окно согласия (первый запуск)
- [x] Чекбокс для минтинга
- [ ] Чекбокс для вывода токенов (нужно добавить в код вывода)
- [ ] Тестирование на Devnet

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. **Протестировать модальное окно:**
   - Открыть `tamagotchi-game.html` в режиме инкогнито
   - Проверить, что модальное окно показывается
   - Согласиться и проверить, что больше не показывается

2. **Протестировать чекбокс минтинга:**
   - Открыть `mint.html`
   - Попробовать сминтить NFT
   - Проверить, что показывается модальное окно

3. **Добавить чекбокс к выводу токенов:**
   - Найти функцию вывода в боте или на сайте
   - Добавить `await LegalCheckbox.showConsentDialog('withdraw')`

4. **Деплой:**
   - Запушить все файлы
   - Проверить на live версии

---

## 📂 ФАЙЛЫ

### Созданы:
- `terms.html` - Terms of Service
- `privacy.html` - Privacy Policy
- `disclaimer.html` - Risk Warning
- `js/legal-consent.js` - модальное окно первого запуска
- `js/legal-checkbox.js` - чекбокс для форм

### Обновлены:
- `index.html` - footer
- `whitepaper.html` - footer
- `mint.html` - footer + legal-checkbox.js
- `profile.html` - footer
- `tamagotchi-game.html` - footer + legal-consent.js
- `referral.html` - footer
- `bot/bot.py` - welcome message

---

## ✅ ГОТОВО К MAINNET

Все критические компоненты для Mainnet готовы!

Осталось только:
1. Протестировать
2. Добавить чекбокс к выводу токенов (если еще не добавлен)
3. Задеплоить

**Удачи с Mainnet! 🚀**

