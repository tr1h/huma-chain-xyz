# 🖼️ Как добавить картинку NFT на Solscan

**Проблема:** На Solscan показывается placeholder "NFT" вместо реальной картинки.

**Причина:** При минте NFT нужно указать правильный IPFS URL изображения в метаданных.

---

## ✅ Решение:

### 1. **IPFS URLs уже есть в коде!**

В `mint.html` и `my-nfts.html` есть функция `getNFTImageUrl()` которая возвращает IPFS URLs:
- Bronze Common: `https://gateway.lighthouse.storage/ipfs/bafkreidvxzsnozwpgjqbydcncpumcgk3aqmr3evxhqjmf6ibzmrmuv565i`
- Bronze Uncommon: `https://gateway.lighthouse.storage/ipfs/bafkreibnoiown4k6dyhxvv642ep6av6xwkgtqvusrhhn7l4janrgfjixbq`
- И т.д.

### 2. **Проверка изображений:**

Открой IPFS URL в браузере:
```
https://gateway.lighthouse.storage/ipfs/bafkreidvxzsnozwpgjqbydcncpumcgk3aqmr3evxhqjmf6ibzmrmuv565i
```

Если изображение загружается - ✅ все работает!
Если 404 или ошибка - нужно загрузить изображения на IPFS.

### 3. **Как работает минт:**

1. При минте NFT вызывается `mintOnChainNFTAsync()` в `mint.html`
2. Функция получает `imageUrl` через `getNFTImageUrl(tier, rarity)`
3. `imageUrl` отправляется в API `mint-nft-onchain.js`
4. API создает метаданные с полем `image: imageUrl`
5. Метаданные загружаются на Arweave
6. NFT минтится с URI метаданных

### 4. **Почему на Solscan placeholder?**

Возможные причины:
- ❌ Изображение не загружено на IPFS (404 ошибка)
- ❌ IPFS gateway недоступен
- ❌ Метаданные не загружены на Arweave
- ❌ При минте использовался неправильный `imageUrl`

### 5. **Как исправить существующий NFT:**

**Вариант 1: Обновить метаданные (если NFT MUTABLE)**
```javascript
// В Solscan видно что NFT MUTABLE - можно обновить!
// Нужно вызвать updateMetadata через Metaplex
```

**Вариант 2: Заминтить новый NFT**
- Новые NFT будут использовать правильные IPFS URLs
- Старые NFT останутся с placeholder (если не обновить)

---

## 🔧 Что нужно сделать:

### ✅ Уже сделано:
1. IPFS URLs добавлены в `getNFTImageUrl()`
2. Функция используется при минте
3. Legendary для Bronze добавлен (временно использует Common image)

### ⚠️ Нужно проверить:
1. **Доступность изображений на IPFS:**
   - Открой каждый IPFS URL в браузере
   - Убедись что изображения загружаются
   - Проверь размер (должно быть 1000x1000 или 512x512)

2. **Загрузить Legendary изображение для Bronze:**
   - Сейчас используется Common image как placeholder
   - Нужно создать и загрузить реальное Legendary изображение

3. **Обновить существующие NFT (опционально):**
   - Если NFT MUTABLE - можно обновить метаданные
   - Если IMMUTABLE - нужно заминтить новый NFT

---

## 📝 Проверка:

1. **Открой IPFS URL в браузере:**
   ```
   https://gateway.lighthouse.storage/ipfs/bafkreidvxzsnozwpgjqbydcncpumcgk3aqmr3evxhqjmf6ibzmrmuv565i
   ```

2. **Проверь метаданные NFT на Solscan:**
   - Открой NFT на Solscan
   - Перейди в "Metadata" tab
   - Проверь поле `image` - должен быть IPFS URL

3. **Проверь в Phantom Wallet:**
   - NFT должен показывать изображение автоматически
   - Если placeholder - значит IPFS URL недоступен

---

## 🚀 Следующие шаги:

1. ✅ Проверить доступность IPFS изображений
2. ✅ Загрузить Legendary изображение для Bronze (если нужно)
3. ✅ Заминтить тестовый NFT и проверить что изображение показывается
4. ⚠️ Обновить существующие NFT (если нужно и возможно)

---

**Важно:** После исправления новые NFT будут показывать изображения правильно. Старые NFT нужно обновить вручную (если они MUTABLE).

