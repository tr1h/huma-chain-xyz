# 📱 Phantom Mobile Deep Link - Реализация

## ✅ Что сделано:

### 1. Deep Link для мобильного Phantom кошелька
При нажатии на "Mint with SOL" в Telegram WebApp:
- Создается deep link: `https://phantom.app/ul/v1/connect?app_url=...&redirect_link=...`
- Пытается открыть Phantom кошелек на мобильном
- Если Phantom не установлен, открывается страница минтинга в браузере

### 2. Автоматический выбор тира
- URL содержит параметр `tier=Bronze|Silver|Gold|Platinum|Diamond`
- На странице `mint.html` можно автоматически выбрать тир из URL

### 3. Разный UI для Telegram и браузера
- В Telegram WebApp: показывается выбор тира с кнопками, которые открывают Phantom
- В браузере: обычный процесс минтинга через подключенный кошелек

---

## 🔧 Как это работает:

### В Telegram WebApp:
1. Пользователь нажимает "Mint with SOL"
2. Показывается модальное окно с выбором тира
3. При выборе тира:
   - Создается deep link для Phantom
   - Пытается открыть Phantom кошелек
   - Если не получается, открывается `mint.html` в браузере

### Deep Link формат:
```
https://phantom.app/ul/v1/connect?app_url={mintUrl}&redirect_link={mintUrl}
```

Где `mintUrl` = `https://solanatamagotchi.com/mint.html?user_id=...&tier=...`

---

## 📝 Что нужно добавить в `mint.html`:

### Автоматический выбор тира из URL:
```javascript
// В mint.html, при загрузке страницы
const urlParams = new URLSearchParams(window.location.search);
const tierParam = urlParams.get('tier');
const mobileParam = urlParams.get('mobile');

if (tierParam && mobileParam === 'true') {
    // Автоматически выбрать тир и показать кнопку минтинга
    // Можно автоматически вызвать mintSOL(tierParam) если кошелек подключен
}
```

---

## 🎯 Преимущества:

1. **Удобство:** Пользователь может минтить NFT прямо из Telegram
2. **Мобильная поддержка:** Deep link открывает Phantom на мобильном
3. **Fallback:** Если Phantom не установлен, открывается браузер
4. **Автоматизация:** Тип NFT выбирается автоматически из URL

---

## ⚠️ Ограничения:

- Deep link работает только если Phantom установлен на устройстве
- На iOS может потребоваться разрешение на открытие внешних приложений
- Если Phantom не установлен, пользователь увидит страницу в браузере

---

**Готово к использованию!** ✅

