# 👤 Profile Page & Authentication Setup

## ✅ Что добавлено:

### 1. **Страница профиля (`profile.html`)**
- Авторизация через Telegram Login Widget
- Привязка Twitter/X аккаунта
- Подключение Phantom Wallet
- Синхронизация данных между Telegram и сайтом
- Ретро-дизайн в стиле главной страницы

### 2. **Навигация**
- Кнопка "👤 Profile" добавлена на главную страницу
- Ссылка в футере на всех страницах

### 3. **База данных**
- SQL скрипт для добавления колонки `twitter_username` в таблицу `leaderboard`

---

## 🚀 Настройка:

### Шаг 1: Добавить колонку в базу данных

Выполни SQL скрипт в Supabase SQL Editor:

```sql
-- Файл: sql/add_twitter_username_column.sql
```

Или выполни вручную:

```sql
ALTER TABLE leaderboard ADD COLUMN IF NOT EXISTS twitter_username TEXT;
CREATE INDEX IF NOT EXISTS idx_leaderboard_twitter_username 
ON leaderboard(twitter_username);
```

### Шаг 2: Настроить Telegram Login Widget

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь команду `/mybots`
3. Выбери бота `@GotchiGameBot`
4. Выбери "Bot Settings" → "Domain"
5. Добавь домен: `solanatamagotchi.com`
6. Подтверди настройку

**Важно:** Telegram Login Widget работает только на домене, указанном в BotFather!

### Шаг 3: Проверить работу

1. Открой `https://solanatamagotchi.com/profile.html`
2. Нажми кнопку Telegram Login Widget
3. Авторизуйся через Telegram
4. Профиль должен загрузиться автоматически

---

## 📋 Функционал:

### **Авторизация через Telegram:**
- Использует официальный Telegram Login Widget
- Автоматически получает `telegram_id`, `username`, `first_name`
- Сохраняет данные в `localStorage` для быстрого доступа
- Синхронизирует с базой данных `leaderboard`

### **Привязка Twitter:**
- Пользователь вводит Twitter username (без @)
- Сохраняется в колонке `twitter_username` таблицы `leaderboard`
- Можно отвязать в любой момент

### **Подключение Phantom Wallet:**
- Подключает Phantom Wallet через Solana Web3.js
- Сохраняет `wallet_address` в базе данных
- Связывает кошелек с Telegram аккаунтом
- Можно отключить в любой момент

### **Синхронизация данных:**
- Кнопка "🔄 SYNC NOW" обновляет данные из базы
- Загружает актуальный баланс TAMA
- Обновляет информацию о кошельке
- Показывает все связанные аккаунты

---

## 🔧 Технические детали:

### **Telegram Login Widget:**
```html
<script async src="https://telegram.org/js/telegram-widget.js?22" 
    data-telegram-login="GotchiGameBot" 
    data-size="large" 
    data-onauth="onTelegramAuth(user)" 
    data-request-access="write">
</script>
```

### **Структура данных в базе:**
```sql
leaderboard
├─ telegram_id (PK)
├─ telegram_username
├─ wallet_address
├─ twitter_username  -- НОВАЯ КОЛОНКА
├─ tama
└─ ...
```

### **LocalStorage:**
- `telegram_id` - ID пользователя Telegram
- `telegram_username` - Username пользователя
- `telegram_first_name` - Имя пользователя

---

## 🎨 Дизайн:

- Ретро-стиль в духе главной страницы
- Шрифт "Press Start 2P" для заголовков и кнопок
- Пиксельные эффекты и анимации
- Градиенты и тени в стиле игры
- Адаптивный дизайн для мобильных устройств

---

## 🔐 Безопасность:

1. **Telegram Widget** - официальный виджет от Telegram, безопасен
2. **Supabase RLS** - Row Level Security защищает данные
3. **LocalStorage** - только для удобства, не критичные данные
4. **Wallet Connection** - требует подтверждение пользователя

---

## 📝 TODO (опционально):

- [ ] Добавить OAuth для Twitter (вместо ручного ввода)
- [ ] Добавить историю синхронизаций
- [ ] Добавить экспорт данных профиля
- [ ] Добавить настройки приватности
- [ ] Добавить уведомления о синхронизации

---

## ✅ Готово к использованию!

После выполнения SQL скрипта и настройки домена в BotFather, страница профиля полностью функциональна!

