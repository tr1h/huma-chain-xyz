# 🌐 Настройка домена solanatamagotchi.com

## 📋 Текущая ситуация

**Домен:** `solanatamagotchi.com`  
**Регистратор:** Namecheap  
**Статус:** ACTIVE (Nov 12, 2025 - Nov 12, 2026)  
**DNS:** Namecheap BasicDNS  
**Проблема:** DNS записи не работают через сутки после добавления

---

## 🔍 Как проверить DNS записи

### 1. Онлайн проверка (самый простой способ)

Откройте эти сайты в браузере:

- **https://dnschecker.org/#A/solanatamagotchi.com** - проверит A записи по всему миру
- **https://www.whatsmydns.net/#A/solanatamagotchi.com** - визуальная карта распространения DNS
- **https://mxtoolbox.com/DNSLookup.aspx** - детальная проверка всех записей

### 2. Через командную строку (Windows)

```powershell
# Проверка A записей
nslookup solanatamagotchi.com

# Проверка через Google DNS
nslookup solanatamagotchi.com 8.8.8.8

# Проверка всех типов записей
nslookup -type=ANY solanatamagotchi.com
```

### 3. Проверка через PowerShell (более детально)

```powershell
# Проверка A записей
Resolve-DnsName solanatamagotchi.com -Type A

# Проверка всех записей
Resolve-DnsName solanatamagotchi.com -Type ANY
```

---

## ⚠️ Проблема: Нет DNS записей в Advanced DNS

**Из скриншота видно:**
- В разделе "HOST RECORDS" написано **"No Records Found"**
- Это значит что DNS записи **НЕ ДОБАВЛЕНЫ** или **НЕ СОХРАНЕНЫ**

---

## ✅ Решение: Добавить DNS записи правильно

### Вариант 1: GitHub Pages (для статического сайта)

Если ваш сайт на GitHub Pages (`tr1h.github.io/huma-chain-xyz`):

1. **В Namecheap → Advanced DNS → HOST RECORDS → ADD NEW RECORD:**

   **Запись 1 (A запись):**
   - **Type:** `A Record`
   - **Host:** `@`
   - **Value:** `185.199.108.153`
   - **TTL:** `Automatic`

   **Запись 2 (A запись):**
   - **Type:** `A Record`
   - **Host:** `@`
   - **Value:** `185.199.109.153`
   - **TTL:** `Automatic`

   **Запись 3 (A запись):**
   - **Type:** `A Record`
   - **Host:** `@`
   - **Value:** `185.199.110.153`
   - **TTL:** `Automatic`

   **Запись 4 (A запись):**
   - **Type:** `A Record`
   - **Host:** `@`
   - **Value:** `185.199.111.153`
   - **TTL:** `Automatic`

   **Запись 5 (CNAME для www):**
   - **Type:** `CNAME Record`
   - **Host:** `www`
   - **Value:** `tr1h.github.io`
   - **TTL:** `Automatic`

2. **В GitHub репозитории:**
   - Settings → Pages → Custom domain
   - Добавьте `solanatamagotchi.com`
   - Включите "Enforce HTTPS"

### Вариант 2: Render.com (для API)

Если ваш API на Render.com (`huma-chain-xyz.onrender.com`):

1. **В Render.com:**
   - Settings → Custom Domains
   - Добавьте `solanatamagotchi.com`
   - Render даст вам IP адреса или CNAME

2. **В Namecheap → Advanced DNS:**

   **Если Render дал IP адреса (A записи):**
   - Добавьте A записи с IP адресами от Render

   **Если Render дал CNAME:**
   - **Type:** `CNAME Record`
   - **Host:** `@` или `api`
   - **Value:** `huma-chain-xyz.onrender.com`
   - **TTL:** `Automatic`

---

## 🔧 Пошаговая инструкция для Namecheap

1. **Зайдите в Namecheap:**
   - https://www.namecheap.com/myaccount/login/
   - Domains → Domain List → `solanatamagotchi.com` → Manage

2. **Перейдите в Advanced DNS:**
   - Вкладка "Advanced DNS"

3. **Добавьте записи:**
   - Нажмите **"ADD NEW RECORD"** (красная кнопка с плюсом)
   - Выберите тип записи (A или CNAME)
   - Заполните поля
   - **ВАЖНО:** Нажмите галочку ✅ чтобы сохранить

4. **Проверьте что записи сохранились:**
   - После добавления записи должны появиться в таблице
   - Если их нет - вы забыли нажать галочку!

5. **Подождите распространения DNS:**
   - Обычно: **5-30 минут**
   - Максимум: **24-48 часов**
   - Если через сутки не работает - что-то не так

---

## 🚨 Частые ошибки

### ❌ Ошибка 1: Записи не сохранились
**Причина:** Не нажали галочку ✅ после добавления  
**Решение:** Добавьте записи заново и обязательно нажмите ✅

### ❌ Ошибка 2: Неправильный Host
**Причина:** Для корневого домена нужно `@`, не `solanatamagotchi.com`  
**Решение:** Используйте `@` для корневого домена

### ❌ Ошибка 3: Используется BasicDNS вместо Custom DNS
**Причина:** Namecheap BasicDNS может не поддерживать все типы записей  
**Решение:** Убедитесь что используете "Custom DNS" или "Advanced DNS"

### ❌ Ошибка 4: DNS еще не распространился
**Причина:** DNS изменения могут занимать до 48 часов  
**Решение:** Проверьте через https://dnschecker.org/ - если везде одинаковые записи, значит распространилось

---

## 📊 Как проверить что все работает

### 1. Проверка DNS записей:
```powershell
# Должны вернуться IP адреса
Resolve-DnsName solanatamagotchi.com -Type A
```

### 2. Проверка доступности сайта:
```powershell
# Должен вернуть HTTP 200
curl -I https://solanatamagotchi.com
```

### 3. Онлайн проверка:
- https://dnschecker.org/#A/solanatamagotchi.com
- https://www.whatsmydns.net/#A/solanatamagotchi.com

---

## 🎯 Рекомендации

1. **Для GitHub Pages:** Используйте A записи (4 штуки) + CNAME для www
2. **Для Render.com:** Используйте CNAME (проще и надежнее)
3. **Проверяйте через dnschecker.org** - покажет распространение по всему миру
4. **Подождите минимум 30 минут** после добавления записей
5. **Если через 24 часа не работает** - проверьте что записи действительно сохранены в Namecheap

---

## 📞 Если ничего не помогает

1. **Проверьте что записи есть в Namecheap:**
   - Advanced DNS → HOST RECORDS
   - Должны быть видны все добавленные записи

2. **Очистите DNS кеш:**
   ```powershell
   ipconfig /flushdns
   ```

3. **Попробуйте другой DNS сервер:**
   - Используйте Google DNS (8.8.8.8) или Cloudflare (1.1.1.1)

4. **Свяжитесь с Namecheap Support:**
   - Они могут проверить настройки на своей стороне

---

**Последнее обновление:** 13 ноября 2024

