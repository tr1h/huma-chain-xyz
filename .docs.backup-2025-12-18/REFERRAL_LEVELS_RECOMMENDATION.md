# 🔗 REFERRAL LEVELS - РЕКОМЕНДАЦИИ

## 📊 ТЕКУЩАЯ СИСТЕМА: 1 УРОВЕНЬ

### ✅ Что работает сейчас:

**1 уровень = Прямые рефералы:**
```
User A → приглашает → User B
User A получает: 1,000 TAMA
```

**Награды:**
- 1,000 TAMA за каждого прямого реферала
- Milestone бонусы:
  - 5 рефералов → +1,000 TAMA
  - 10 рефералов → +3,000 TAMA
  - 25 рефералов → +10,000 TAMA
  - 50 рефералов → +30,000 TAMA
  - 100 рефералов → +100,000 TAMA

**База данных:**
```sql
referrals (
  referrer_telegram_id,  -- Кто пригласил
  referred_telegram_id,  -- Кого пригласили
  reward_given = 1000    -- Награда
)
```

---

## 🤔 НУЖНА ЛИ МНОГОУРОВНЕВАЯ СИСТЕМА?

### ❌ ПРОТИВ 2+ уровней:

1. **Юридические риски:**
   - MLM (Multi-Level Marketing) незаконен в некоторых странах
   - Может быть классифицирован как пирамида
   - SEC и регуляторы враждебны к MLM в крипте

2. **Сложность:**
   - Труднее понять для пользователей
   - Сложнее программировать
   - Больше багов и крайних случаев

3. **Экономика:**
   - Быстрее истощает пул TAMA
   - Сложнее балансировать награды
   - Может привести к инфляции

4. **Репутация:**
   - Выглядит как "пирамида"
   - Отпугивает серьезных пользователей
   - Негативное восприятие в крипто-комьюнити

### ✅ ЗА 2 уровня (осторожно!):

1. **Вирусный рост:**
   - Больше мотивации приглашать активных людей
   - Сетевой эффект

2. **Больше наград:**
   - Пользователи получают пассивный доход

**НО:** Все преимущества можно получить БЕЗ второго уровня!

---

## 💡 РЕКОМЕНДАЦИИ:

### ⭐ ВАРИАНТ 1: Улучшенная 1-уровневая система (РЕКОМЕНДУЕТСЯ!)

**Что добавить:**

#### 1. **Activity-based rewards (награды за активность рефералов):**
```python
# Вместо 2-го уровня, награждай за АКТИВНОСТЬ рефералов!

if referred_user_earned_tama >= 10000:
    # Реферер получает 10% от заработка реферала (один раз)
    bonus = referred_user_earned_tama * 0.1
    award_bonus(referrer, bonus, "Active Referral Bonus")
```

**Преимущества:**
- Мотивирует приглашать АКТИВНЫХ пользователей
- Не выглядит как пирамида
- Юридически безопаснее

#### 2. **Team-based rewards (командные награды):**
```python
# Когда ВСЯ команда достигает целей:
total_team_tama = sum(referral.tama for referral in user.referrals)

if total_team_tama >= 100000:
    award_team_bonus(user, 5000)  # Бонус за активную команду
```

#### 3. **Streak bonuses (бонусы за регулярность):**
```python
# Реферер получает бонус если рефералы играют регулярно
active_referrals_count = count_active_last_7_days(user.referrals)

if active_referrals_count >= 5:
    award_streak_bonus(user, 2000)  # Еженедельный бонус
```

#### 4. **Quality rewards (награды за качество):**
```python
# Больше награда за рефералов с кошельками
if referred_user.has_wallet:
    bonus_reward = 500  # Дополнительно 500 TAMA
elif referred_user.bought_nft:
    bonus_reward = 2000  # Дополнительно 2000 TAMA
```

---

### ⚠️ ВАРИАНТ 2: Строго ограниченная 2-уровневая система

**Если ОЧЕНЬ хочется 2 уровня:**

**Правила (чтобы избежать пирамиды):**

1. **Маленький процент:**
   ```python
   # Level 1: 1,000 TAMA (как сейчас)
   # Level 2: 100 TAMA (10% от Level 1) - ТОЛЬКО раз
   ```

2. **Одноразовая награда:**
   - Level 2 награда дается ОДИН РАЗ
   - Не накапливается
   - Максимум 100 TAMA за Level 2

3. **Требование активности:**
   ```python
   # Level 2 награда только если:
   if level1_referral.earned_tama >= 5000:  # Реферал активен
       award_level2_bonus(referrer, 100)
   ```

4. **Прозрачность:**
   - Четко показывать в UI что это 2 уровня
   - Объяснять почему это не пирамида
   - Лимит на общую сумму Level 2 наград

**Структура БД:**
```sql
referrals (
  id,
  referrer_telegram_id,    -- User A
  referred_telegram_id,    -- User B
  level INTEGER DEFAULT 1, -- 1 или 2
  reward_given INTEGER,
  parent_referral_id       -- Ссылка на Level 1 реферал
)
```

---

## 🎯 МОЯ РЕКОМЕНДАЦИЯ: ВАРИАНТ 1

**Почему:**

1. ✅ **Юридически безопасно**
2. ✅ **Лучше мотивирует** (фокус на активности)
3. ✅ **Проще понять** пользователям
4. ✅ **Гибче настраивать** награды
5. ✅ **Нет риска "пирамиды"**

**Реализация:**

### 1. Activity Bonus (ПРОСТОЙ И ЭФФЕКТИВНЫЙ!)
```python
# В handle_start или при начислении TAMA:

def check_referral_activity_bonus(referred_user_id):
    # Получить реферера этого юзера
    referral = supabase.table('referrals').select('*').eq('referred_telegram_id', referred_user_id).execute()
    
    if not referral.data:
        return
    
    referrer_id = referral.data[0]['referrer_telegram_id']
    
    # Получить заработок реферала
    referred_user = supabase.table('leaderboard').select('tama').eq('telegram_id', referred_user_id).execute()
    referred_tama = referred_user.data[0]['tama'] if referred_user.data else 0
    
    # Проверить милстоуны активности
    milestones = [
        (5000, 500, 'active_5k'),
        (10000, 1000, 'active_10k'),
        (25000, 2500, 'active_25k'),
        (50000, 5000, 'active_50k')
    ]
    
    for threshold, bonus, milestone_id in milestones:
        if referred_tama >= threshold:
            # Проверить не давали ли этот бонус уже
            if not check_milestone_given(referrer_id, referred_user_id, milestone_id):
                # Начислить бонус реферу
                award_tama(referrer_id, bonus, f"Active Referral Milestone: {milestone_id}")
                mark_milestone_given(referrer_id, referred_user_id, milestone_id)
                
                # Уведомить
                bot.send_message(
                    referrer_id,
                    f"🎉 Your referral earned {threshold:,} TAMA!\n"
                    f"💰 You received {bonus:,} TAMA bonus!"
                )
```

### 2. Weekly Team Bonus
```python
# Еженедельная задача для активных реферов
def calculate_weekly_team_bonus():
    # Для каждого реферера
    for user in all_users_with_referrals:
        # Посчитать активных рефералов за неделю
        active_count = count_active_referrals_last_7_days(user.telegram_id)
        
        if active_count >= 3:
            bonus = active_count * 100  # 100 TAMA за каждого активного
            award_weekly_team_bonus(user.telegram_id, bonus)
```

---

## 📝 ЧТО ДОБАВИТЬ В АДМИНКУ:

1. **Вкладка "Activity Rewards":**
   - Показать какие рефералы активны
   - Сколько бонусов начислено за активность
   - График активности команды

2. **Настройки Activity Milestones:**
   - Редактировать пороги (5k, 10k, 25k, 50k)
   - Редактировать награды (500, 1000, 2500, 5000)

3. **Team Performance Dashboard:**
   - Топ рефереров по активности команды
   - Средний заработок на реферала
   - Процент активных рефералов

---

## 🎮 ПРИМЕР СООБЩЕНИЯ ПОЛЬЗОВАТЕЛЮ:

```
🔗 Your Referral System:

💰 Direct Rewards:
• 1,000 TAMA for each friend instantly!

🎁 Milestone Bonuses:
• 5 referrals → +1,000 TAMA
• 10 referrals → +3,000 TAMA
• 25 referrals → +10,000 TAMA
• 50 referrals → +30,000 TAMA
• 100 referrals → +100,000 TAMA

⭐ Activity Bonuses (NEW!):
• Friend earns 5,000 TAMA → You get +500 TAMA
• Friend earns 10,000 TAMA → You get +1,000 TAMA
• Friend earns 25,000 TAMA → You get +2,500 TAMA
• Friend earns 50,000 TAMA → You get +5,000 TAMA

🏆 Team Bonuses:
• 3+ active friends/week → +300 TAMA weekly bonus
• 5+ active friends/week → +500 TAMA weekly bonus
• 10+ active friends/week → +1,000 TAMA weekly bonus
```

---

## 💡 ИТОГ:

**НЕ ДЕЛАЙ 2 уровня - добавь Activity-based rewards!**

Это:
- ✅ Безопаснее юридически
- ✅ Лучше мотивирует
- ✅ Больше TAMA для активных рефереров
- ✅ Не выглядит как пирамида
- ✅ Проще балансировать экономику

**Хочешь я реализую?** 🚀

