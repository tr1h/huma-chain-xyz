# 🔍 Проверка NFT Metadata URI

## ❌ Проблема:

NFT показывает `json_uri: "https://mockstorage.example.com/..."` вместо Arweave URI.

Это означает, что при минтинге использовался **mockStorage** вместо реального **Bundlr/Arweave**.

## ✅ Решение:

### **1. Проверить транзакцию обновления:**

Откройте транзакцию обновления:
```
https://solscan.io/tx/3etmZh1iKk874j2XssPsV8BJmaRstCUM8Hc1AXTEGRUPKidMXZs2sMQe4wYM8a7i5VbYHVwDFbib5XBmKoVGhjXW?cluster=devnet
```

В транзакции должно быть видно:
- **Update Metadata** инструкция
- Новый **URI** (должен быть на Arweave, не mockStorage)

### **2. Проверить NFT после обновления:**

Откройте NFT:
```
https://solscan.io/token/A9Mitqhotvty33zG8LWmEegBVnNx47xz1AfjU3z43amc?cluster=devnet
```

**Ожидаемый результат:**
- `json_uri` должен быть на Arweave: `https://arweave.net/...`
- НЕ должен быть: `https://mockstorage.example.com/...`

### **3. Если все еще mockStorage:**

**Причины:**
1. Bundlr не инициализируется правильно на Render.com
2. Недостаточно SOL на payer keypair для Arweave fees
3. Bundlr devnet недоступен

**Решение:**
1. Проверить баланс payer keypair (нужно минимум 0.01 SOL)
2. Проверить логи Render.com сервиса
3. Убедиться, что `@metaplex-foundation/js` установлен с Bundlr поддержкой

---

## 🔧 Исправления в коде:

### **1. Запрет mockStorage:**

Код теперь **НЕ использует mockStorage** как fallback:
- Если Bundlr не работает → выбрасывается ошибка
- Не создаются NFT с mockStorage URI

### **2. Проверка URI:**

После загрузки метаданных проверяется:
- URI не должен содержать `mockstorage.example.com`
- Должен быть на Arweave: `arweave.net`

---

## 📋 Что проверить:

1. ✅ Транзакция обновления успешна
2. ✅ Новый URI на Arweave (не mockStorage)
3. ✅ Solscan обновил кэш (подождать 1-2 минуты)
4. ✅ Изображение отображается

---

## 🚨 Если проблема остается:

1. **Проверить логи Render.com:**
   - Сервис: `solanatamagotchi-onchain`
   - Искать ошибки Bundlr инициализации

2. **Проверить баланс payer:**
   ```bash
   solana balance <PAYER_PUBKEY> --url devnet
   ```

3. **Проверить Bundlr статус:**
   - Devnet: https://devnet.bundlr.network
   - Mainnet: https://node1.bundlr.network

---

## ✅ Ожидаемый результат:

После обновления NFT должен иметь:
```json
{
  "json_uri": "https://arweave.net/...",
  "metadata": {
    "name": "Gotchi Bronze Common #160",
    "image": "https://gateway.lighthouse.storage/ipfs/..."
  }
}
```

**НЕ должно быть:**
```json
{
  "json_uri": "https://mockstorage.example.com/..."
}
```












