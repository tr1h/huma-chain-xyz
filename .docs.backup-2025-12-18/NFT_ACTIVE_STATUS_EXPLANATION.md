# 🔍 Почему NFT могут быть неактивными?

## 📊 Текущая ситуация:

### **Как работает `is_active`:**

1. **При создании NFT:**
   - Всегда устанавливается `is_active = true`
   - Код: `api/mint-nft-sol.php`, `nft-mint.html`, `api/mint-nft-onchain.js`

2. **Когда NFT может стать неактивным:**
   - ❌ **Off-Chain NFT** (виртуальные, не на блокчейне) - деактивируются при миграции
   - ❌ **Старые тестовые NFT** - деактивируются для очистки
   - ❌ **Вручную в базе данных** - администратор может деактивировать

3. **Проблема:**
   - Если NFT **на блокчейне** (on-chain), то он **НИКОГДА** не должен быть неактивным!
   - NFT на блокчейне существует навсегда - его нельзя "деактивировать"
   - `is_active` - это только флаг в базе данных, не влияет на блокчейн

---

## 🎯 Решение:

### **Логика должна быть такой:**

```javascript
// Если NFT на блокчейне - он ВСЕГДА активен!
const isOnChain = nft.nft_mint_address && 
    nft.nft_mint_address.length > 30 && 
    !nft.nft_mint_address.includes('_');

// On-chain NFT = всегда активен, независимо от is_active в БД
const isActive = isOnChain ? true : (nft.is_active !== false);
```

**Почему:**
- ✅ NFT на блокчейне существует навсегда
- ✅ Его нельзя "деактивировать" - он в блокчейне
- ✅ `is_active` в БД - это только для Off-Chain NFT
- ✅ On-chain NFT всегда должны давать boost

---

## 🔧 Что нужно исправить:

1. **В `loadNFTCollection()`:**
   - Если NFT on-chain → всегда активен
   - Если NFT off-chain → используем `is_active` из БД

2. **В `showNFTDetail()`:**
   - Та же логика

3. **В расчете boost:**
   - On-chain NFT всегда учитываются
   - Off-chain NFT учитываются только если `is_active = true`

---

## 📋 Итог:

**Ответ на вопрос:** NFT на блокчейне **НЕ МОГУТ** быть неактивными! 

Если NFT имеет реальный Solana адрес (nft_mint_address), то он:
- ✅ Всегда существует на блокчейне
- ✅ Всегда должен быть активным в игре
- ✅ Всегда должен давать boost

`is_active` в базе данных - это только для:
- Off-Chain NFT (виртуальных)
- Старых тестовых NFT
- Управления видимостью в UI

**Но для On-Chain NFT это не имеет смысла!**

