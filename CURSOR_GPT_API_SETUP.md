# ⚙️ Настройка GPT через API в Cursor

## ✅ Да, можно использовать GPT вместо Claude!

Cursor поддерживает разные AI модели, включая GPT-4 через OpenAI API.

---

## 🔧 Как настроить:

### Шаг 1: Получить API ключ OpenAI

1. Зайди на https://platform.openai.com/api-keys
2. Войди в аккаунт (где у тебя оплачен GPT)
3. Создай новый API ключ (или используй существующий)
4. Скопируй ключ (он будет показан только один раз!)

### Шаг 2: Настроить в Cursor

1. Открой Cursor
2. Нажми `Ctrl+Shift+P` (или `Cmd+Shift+P` на Mac)
3. Введи: `Cursor: Open Settings`
4. Или через меню: **File → Preferences → Settings**

5. В настройках найди раздел **"AI"** или **"Models"**

6. Там будет опция для выбора модели:
   - **Claude** (по умолчанию)
   - **GPT-4** (через API)
   - **GPT-3.5** (через API)

7. Выбери **GPT-4** или **GPT-3.5**

8. Введи свой OpenAI API ключ в поле **"API Key"**

9. Сохрани настройки

---

## 💰 Стоимость API:

**Важно:** API стоит отдельно от подписки на ChatGPT!

- **GPT-4:** ~$0.03 за 1K токенов input, ~$0.06 за 1K токенов output
- **GPT-3.5 Turbo:** ~$0.0015 за 1K токенов (гораздо дешевле!)

**Рекомендация:** Используй GPT-3.5 Turbo - он намного дешевле и достаточно хорош для программирования!

---

## 📋 Альтернативный способ:

Если в настройках Cursor нет опции GPT:

1. В Cursor нажми `Ctrl+,` (настройки)
2. В поиске введи: `model` или `api`
3. Найди опцию **"Cursor: Model"** или **"AI Provider"**
4. Измени на **OpenAI** или **GPT-4**

---

## 🔑 Где хранить API ключ:

**Безопасно:**
- Cursor хранит ключ локально (зашифрованно)
- Ключ используется только для API запросов
- Не передается третьим лицам

**⚠️ Важно:**
- Никогда не коммить API ключ в Git!
- Не делиться ключом с другими
- Если ключ скомпрометирован - создай новый на OpenAI

---

## 💡 Сравнение моделей в Cursor:

| Модель | Скорость | Качество | Цена | Рекомендация |
|--------|----------|----------|------|--------------|
| **Claude** | Средняя | Отличное | Включено в Cursor | ✅ По умолчанию |
| **GPT-4** | Медленная | Отличное | Дорого (~$0.03/1K) | Для сложных задач |
| **GPT-3.5** | Быстрая | Хорошее | Дешево (~$0.0015/1K) | ✅ Для программирования |

---

## 🎯 Рекомендация:

### Для экономии денег:
- Используй **GPT-3.5 Turbo** - дешево и достаточно хорошо
- Или оставайся на **Claude** (уже включен в Cursor)

### Для лучшего качества:
- Используй **GPT-4** - но будет дороже
- Или **Claude** - уже хорош и бесплатен в Cursor

---

## ✅ Быстрая настройка:

1. Получи API ключ: https://platform.openai.com/api-keys
2. В Cursor: `Ctrl+,` → Найди "Model" → Выбери "GPT-3.5 Turbo" или "GPT-4"
3. Введи API ключ
4. Готово!

---

## ⚠️ Важно знать:

- **Подписка ChatGPT ≠ API доступ**
- API оплачивается отдельно (по использованию)
- Можно поставить лимит расходов на OpenAI
- GPT-3.5 Turbo намного дешевле GPT-4

---

**Просто помни: GPT-3.5 Turbo достаточно хорош для программирования и в 20 раз дешевле GPT-4! 💰**

